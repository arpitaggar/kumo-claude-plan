# Kumo — Brief vs. Actual Checklist

_Compiled 2026-08-09 from `docs/DEVELOPMENT_ROADMAP.md`, migration history, and a fresh architecture/security/dev-style audit._

## Done (from the v1.0 brief)

- [x] Foundation: auth, itinerary CRUD, Clean Architecture, Riverpod, GoRouter, Material 3
- [x] Collaboration: trip members/roles, invites, real-time chat, typing indicators, read receipts (simplified — last-write-wins JSONB, no vector clocks/event sourcing)
- [x] AI itinerary generation — single-call Claude Haiku, server-side via Edge Function (simplified from "Concierge" agentic mode)
- [x] Expense splitting + ratings (no Stripe Issuing/Connect)
- [x] Social/discovery layer — now a *real* social feed (publish-as-snapshot, fork lineage, likes, follows) superseding the original lightweight Discover tab, plus gamification (XP, levels, 8 badges) closing out the last piece of the original social-layer brief
- [x] B2B — minimal real foundation shipped (orgs, expense approval workflow, cost-center fields) vs. the originally-envisioned full multi-tenant portal

## Remaining from the initial brief (still not built)

- [ ] Concierge AI mode — agentic/streaming generation
- [ ] Virtual debit card (Stripe Issuing) + Stripe Connect settlement
- [ ] Full B2B portal — travel policies, admin dashboard, multi-tenant beyond the current minimal org layer
- [ ] Isar offline-first storage + vector-clock conflict resolution — permanently descoped in favor of SharedPreferences cache + last-write-wins

## Added beyond the initial brief

- [x] Packing lists, trip notes, share sheet, offline banner, home search, profile stats
- [x] Destination-based trip themes (8 presets + auto-suggest) + 4 more visual themes incl. first dark theme
- [x] Trip route segments with pluggable map (flutter_map/OSM default, Google Maps alternate)
- [x] Real routed road/walking geometry for segments (OSRM + Google Directions) — committed `5e4adf0`; stale-geometry-on-edit gap found and fixed (see below)
- [x] Weather forecast chips per trip leg
- [x] Premium feature-flag system + 14-day trial
- [x] Masked, forward-only trip email alias with inbound forwarding
- [x] Work mode: org-scoped trips, expense approval workflow, cost-tracking fields
- [x] macOS build re-enabled for local dev/testing — committed `5e4adf0`
- [x] Trip roles Captain/Crew/Hitchhiker + server-side 18+ age gate at signup — regulatory design keeping Kumo outside COPPA/UK-AADC/GDPR minor-data-controller obligations (see `docs/ARCHITECTURE.md`); Hitchhikers are non-account trip collaborators authenticated via token-based RPCs, added by name only — committed `cd2c40b`/`b6310bc`
- [x] Private `avatars`/`chat-attachments` Storage buckets + signed-URL resolution layer (`KumoAvatar`, `signedStorageUrlProvider`), replacing public-read — committed `cd2c40b`

## Code-complete but not live (deployment gaps)

- [ ] Katha AI generation — needs `ANTHROPIC_API_KEY` secret + Edge Function deploy
- [ ] Push notifications — Android needs Firebase secret + deploy; iOS gated on APNs key + Xcode capability
- [ ] Google Maps tiles — needs a real API key (placeholder only)
- [ ] Masked email inbound delivery — needs a domain + inbound-email provider + webhook secret
- [ ] GitHub Pages for legal docs — not enabled
- [ ] Migrations `stage42_trip_email_log_retention.sql` through `stage45_hitchhikers.sql` (retention purge, private storage buckets, age gate, Hitchhiker RPCs) — not yet confirmed run against the live database. **`stage43` (private buckets) and the app build that depends on it must ship together** — an old client calling the pre-signed `getPublicUrl()` result directly gets a 403 once the buckets flip in production.

## Process/quality gaps surfaced by this audit

- [x] Fix the auth-repo catch-swallow bug in `lib/features/auth/data/repositories/auth_repository_impl.dart` (uncommitted `LocalStorageException` fallback can throw uncaught) — extracted into a `_userAfterCacheFailure()` helper with its own try/catch
- [x] Run `dart format .` (user ran it — 206/338 files reformatted) and install the `dart format`/`flutter analyze` pre-commit hook CLAUDE.md claims exists — added `scripts/hooks/pre-commit` + `scripts/install-git-hooks.sh` (tracked source, since git doesn't sync `.git/hooks/`) and installed it locally
- [x] Update `docs/SECURITY_AUDIT.md`, `docs/ARCHITECTURE.md`, `docs/SOLID_AUDIT.md`, and `lib/core/maps/CLAUDE.md` — all updated with 2026-08-09 findings/addenda (see below)
- [x] Fix unclosed `Sink` in `test/features/chat/presentation/providers/chat_provider_test.dart` — false positive (every caller closes it), suppressed with a targeted `// ignore: close_sinks` + comment explaining why
- [x] Fix raw exception text shown on-screen by `StartupErrorApp` in release builds — gated behind `kReleaseMode` (SEC-030)
- [x] Fix non-constant-time webhook-secret comparison in `supabase/functions/inbound-trip-email/index.ts` — added a `timingSafeEqual` helper (SEC-031)
- [x] Document the 3 work-mode RLS security-review findings (commit `ac53cf3`) in `docs/SECURITY_AUDIT.md` as SEC-026 through SEC-029, plus SEC-030/031 above

**Verification after fixes:** `flutter analyze` — 118 issues, all info-level, zero warnings/errors. `flutter test` — 471/471 passing. `dart format --set-exit-if-changed .` — clean (only `macos/build/` artifacts, not source, show drift).

## Route-geometry completeness pass (2026-08-09, after the above)

Verified the OSRM/Google Directions feature end-to-end (migration → service → provider dispatch → fetch trigger on add/edit/backfill → map rendering) rather than assuming the committed code was fully wired up. It was, with one real gap:

- [x] **Fixed:** editing a segment's origin/destination/mode left the *previous* routed geometry cached (`TripSegment.copyWith`'s `??` pattern can't null a field, and `updateSegment` writes the stale value straight back) — the map would show a road path from the old pin locations, permanently if the refetch ever failed. Added `TripSegmentRepository.clearRouteGeometry` (narrow patch, same pattern as `updateRouteGeometry`/`setSegmentVisibility`) and call it from `FetchTripSegmentRouteGeometry` before every refetch when the incoming segment already has cached geometry. 4 new tests in `fetch_trip_segment_route_geometry_test.dart`.
- **Open item, not fixed (flagging, not deciding for you):** `OsrmRoutingService` calls OSRM's public demo server (`router.project-osrm.org`), which OSRM's own project documents as evaluation-only — no SLA, rate-limited. Every failure already falls back to the existing straight/curved line (no user-facing error), so this isn't broken, but it's not a production-grade dependency either. Worth a decision before relying on it at real traffic: self-host OSRM, or accept the graceful-degradation behavior as-is for now.

**Re-verified:** `flutter analyze` — 118 issues, still all info-level. `flutter test` — 474/474 passing (+3 net after the 4 new tests). Not yet committed.

## Missing unit test pass (2026-08-09, after the above)

Audited actual test coverage (by grepping which classes are exercised inside `test/`, not just filename matching — the filename heuristic had false positives, e.g. the 4 social read usecases are covered by a combined `fetch_social_reads_usecase_test.dart`). Found and closed real gaps:

- [x] **`AuthRepositoryImpl`** — zero repository-level coverage despite being hand-edited twice this session. New `test/features/auth/data/repositories/auth_repository_impl_test.dart`, 28 tests covering every method, including a regression test for the earlier catch-swallow fix.
- [x] **`TripSegmentRepositoryImpl`** — zero coverage despite the new `clearRouteGeometry` logic above. New `test/features/itinerary/data/repositories/trip_segment_repository_impl_test.dart`, 17 tests covering every method.
- [x] **15 domain usecases with no test at all**: `SendMessageUseCase`, `FetchMyOrganizationsUseCase`, `FetchPendingExpenseApprovalsUseCase`, `FetchOrgMembersUseCase`, `FetchOrgCostFieldsUseCase`, `DeleteRatingUseCase`, `LogoutUseCase`, `FetchTripCostFieldValuesUseCase`, `SetTripCostFieldValuesUseCase`, `FetchItineraryUseCase`, `GenerateItineraryUseCase`, `DeleteExpenseUseCase`, `DeletePackingItemUseCase`, `TogglePackingItemUseCase`, `AddPackingItemUseCase` — one test file each (packing's three combined into `packing_write_usecases_test.dart`, matching the delegator-usecase pattern already established elsewhere in this codebase).

**Significant bug found while writing the `TripSegmentRepositoryImpl` stream test, not something the audit or the route-geometry pass had caught:**

- [x] **`Stream.handleError`'s callback return value is silently discarded — every `watch*` stream repository in the app was dropping realtime errors instead of surfacing them.** All 6 stream-based repositories (`ChatRepositoryImpl`, `PackingRepositoryImpl`, `ExpenseRepositoryImpl`, `ItineraryRepositoryImpl`, `TripSegmentRepositoryImpl`, `RatingRepositoryImpl`) built their `Either`-wrapped stream the same way: `.map(Right.new).handleError((e) => Left(...))`. `Stream.handleError`'s handler is `void Function(Object error)` — a returned `Left(...)` value is simply discarded, not injected into the stream (confirmed with an isolated repro, not just inference). Net effect: a Supabase Realtime connection error on any watched itinerary/chat/expenses/packing-list/trip-segments/ratings stream silently stopped updating instead of ever reaching the `AsyncError` state the UI is built to show (every consuming provider does `.fold((f) => throw Exception(f.message), ...)`, which only fires if a `Left` ever actually arrives). Fixed in all 6 files by switching to `StreamTransformer.fromHandlers`, whose `handleError(error, stackTrace, sink)` can actually call `sink.add(Left(...))`. Added/extended regression tests for all 6 (2 tests each, or folded into the two repos' full test files above) proving the error now reaches subscribers.

**Final verification:** `flutter analyze --no-fatal-infos` — 140 issues (up from 118; all new info-level style nits in the new test files, zero warnings/errors, exit 0. `flutter test` — **561/561 passing** (+87 from the 474 baseline above). `dart format lib/ test/` — clean. Not yet committed.

## Scalability audit + easy-fix remediation (2026-08-09)

Full audit written up separately at `docs/SCALABILITY_AUDIT.md` (12 findings, SCALE-001 through SCALE-012), answering "could this scale like Instagram?" Short answer: no, not without 3 subsystem rewrites, and that's expected at this app's actual stage. All items classified as "easy" (code/query/config, no new infrastructure) are now fixed:

- [x] **SCALE-001 — hot-row like counter.** Dropped the synchronous `like_count`-incrementing trigger; posts now count `post_likes` rows at read time via PostgREST's embedded-resource count (`post_likes(count)`), backed by a new `(user_id, created_at)` index.
- [x] **SCALE-003 — no rate limiting on likes/follows/posts.** Added `BEFORE INSERT` trigger-based limits (60 likes/min, 30 follows/min, 20 posts/hour per user) — the same counting pattern already used for AI-generation rate limits, necessarily a DB trigger here since these are direct client inserts with no Edge Function in front of them.
- [x] **SCALE-004 — unbounded follow-list query.** Capped `fetchFeed`'s `follows` lookup to the 1000 most-recently-followed accounts.
- [x] **SCALE-008 — Explore search only covered the 50 newest posts.** Real server-side search now, via `pg_trgm` + GIN indexes on `title`/`description` (kept substring-match UX rather than switching to word-based full-text search).
- [x] **SCALE-009 — no cursor pagination.** Added keyset pagination (`before: DateTime?`) through the full stack — datasource → repository → usecase → a new `PostFeedNotifier` (replacing the old flat `FutureProvider`s) — plus a working "Load more" button wired into both Discover tabs.
- [x] **SCALE-005 — no image resizing.** `resizedImageUrl()` rewrites Storage URLs to request width/quality-limited variants, wired into every avatar and the chat attachment thumbnail (not the full-screen viewer). **Gated behind `kImageResizingEnabled = false`** — deliberately not flipped on, since image transformation is a paid Supabase add-on and its unenabled-state behavior isn't something to assume without checking the live project first. Safe no-op until then.
- [x] **SCALE-011 — no read replicas/connection pooling.** Not a code change — documented as a Supabase dashboard toggle on the live project, outside what this session can do. See `docs/SCALABILITY_AUDIT.md`.

**New migration:** `docs/supabase_migrations/stage33_social_feed_scale.sql` — ✅ run against the live database (2026-08-09).

**Real bug found mid-implementation, not by the original audit:** `followingFeedProvider` watches `authNotifierProvider`; auth resolving through `AuthInitial` → `AuthLoading` → `AuthAuthenticated` on startup rebuilds-and-disposes the feed notifier more than once, and a stale in-flight fetch from an earlier instance could throw trying to set `state` after disposal. Fixed with `mounted` guards in `PostFeedNotifier` after every `await` — this was a real crash risk in the running app, caught by writing a correct test for the new pagination provider, not a test-harness artifact.

**Verification:** `flutter analyze --no-fatal-infos` — 144 issues, all info-level, zero warnings/errors. `flutter test` — **577/577 passing** (+16 from the 561 baseline: 7 `PostFeedNotifier` tests, 7 `resizedImageUrl`/`transformObjectUrl` tests, 2 model-fallback tests). `dart format lib/ test/` — clean. Not yet committed.

**Still open (deliberately not attempted — real infrastructure, not code fixes):** SCALE-002 (no background job/queue anywhere — the prerequisite for the *proper* long-term version of SCALE-001's counter and for ever scaling push fan-out past trip-sized groups), SCALE-006 (JSONB-array RLS scans — not urgent, group sizes are small), SCALE-007 (synchronous push fan-out — fine for this app's domain), SCALE-010 (Supabase Realtime's connection ceiling — a platform migration, not a bug), SCALE-012 (Nominatim/OSRM aren't production-scale — a provider decision, already flagged in the route-geometry pass above).

## Second scalability pass (2026-08-09, after all of the above)

Re-audited with the benefit of having just built the stage33 fixes and separately read the whole organization/work-mode schema in depth (during the coverage pass). Two findings, one fixed, one documented:

- [x] **SCALE-013 — `itinerary_posts` ended up with two overlapping rate-limit triggers.** The SCALE-003 fix added a 20/hour cap without noticing a 30-second-cooldown trigger already existed on the same table (stage23) — both index-backed and individually correct, but redundant (two scans per publish instead of one) and left two mechanisms for a future reader to reconcile. Folded into one trigger function. New migration `docs/supabase_migrations/stage34_consolidate_post_rate_limits.sql` — **✅ run against the live database (2026-08-11).**
- [ ] **SCALE-014 — `PostFeedNotifier`'s "Load more" accumulates every loaded page in memory forever, documented not fixed.** A real but distant threshold (hundreds of "Load more" taps in one session) and a genuine product trade-off (windowing loses "scroll back up and it's still there"), not something to decide unilaterally — see `docs/SCALABILITY_AUDIT.md` for the standard fix pattern if it ever becomes real.

## Work mode: org join-code system (2026-08-10)

Self-serve onboarding for Work Mode — an org admin generates a code (shown as text + QR), and any authenticated user can redeem it to join that org, optionally scoped to a department (reusing the existing `org_cost_fields`/`org_cost_field_options` structure rather than a new entity). Full design/tradeoff writeup at `/Users/pitto/.claude/plans/synchronous-hopping-matsumoto.md`. Chosen over SSO — no enterprise customer in the pipeline demanding SAML/OIDC, and this schema (`org_members.cost_field_option_id`) is built so SSO could later become a second provisioning path into the same structure rather than a rewrite.

- [x] **New migration `docs/supabase_migrations/stage35_org_join_codes.sql`** — `org_join_codes` table (admin-only SELECT RLS, no direct insert/update/delete policy — all mutation goes through RPCs), `org_members.cost_field_option_id` column + cross-org guard trigger, and 3 `SECURITY DEFINER` RPCs: `generate_org_join_code` (admin-only, collision-checked random code), `revoke_org_join_code` (admin-only), `redeem_org_join_code` (any authenticated user; `FOR UPDATE` row lock for concurrency-safe limited-use redemption, successful-joins-per-hour cap, server-side-only org/user resolution — never trusts a client-supplied id). **✅ Run against the live database (2026-08-10).**
- [x] **Domain/data/presentation layers** — `OrgJoinCode` entity (`isRevoked`/`isExpired`/`isExhausted`/`isActive`), 4 new `OrganizationRepository` methods + usecases, `OrgJoinCodeModel`, datasource/repository impl wiring, `OrgJoinCodesPage` (admin: generate/list/revoke, code shown as copyable text + `qr_flutter` QR), `JoinOrganizationPage` (employee: `mobile_scanner` scan with a synchronous-before-first-`await` re-entrancy guard, or manual entry fallback). New entry points: 4th AppBar icon on `organization_members_page.dart`, "Join with code" action + empty-state link on `organizations_list_page.dart`. Router: `/organizations/join`, `/organizations/:id/join-codes`.
- [x] **New dependencies** `qr_flutter ^4.1.0`, `mobile_scanner ^7.4.0` — iOS `NSCameraUsageDescription` extended to cover scanning (was profile-picture-only), Android `CAMERA` permission + optional camera `<uses-feature>` added.
- [x] **Generalized `deleteCostFieldOption`'s error message** ("This value is in use and can't be deleted") since a join code can now also be the thing blocking deletion via the FK-restrict, not just a trip assignment.
- [x] **Tests** — `org_join_code_test.dart` (getter branching logic), `org_join_code_model_test.dart` (`fromJson`), 4 usecase test files, `organization_repository_impl_test.dart` extended with all 4 methods including one test per distinct RPC-raised message (invalid/revoked/expired/exhausted/already-a-member) confirming `redeemJoinCode` passes each through unchanged.
- **Explicit, acknowledged gap:** `redeem_org_join_code`'s `FOR UPDATE` concurrency correctness (the max-uses=1 double-redeem race) has no automated test — no pgTAP/ephemeral-PG-in-CI setup exists in this repo, same gap as every other RLS/trigger pass this session. Recommend one manual QA pass before this ships: two concurrent redemptions of the same `max_uses=1` code against a live/staging database, confirming exactly one succeeds.
- **Deliberately deferred:** per-department feature-flag overrides and per-department approval-routing/thresholds (this pass only gets people into the right org + department); a `kumo://join?code=...` deep link (no URL-scheme infra exists yet, and scanning inside the app already covers the stated use case); rate-limiting *failed* redeem guesses (not safely buildable in plain Postgres — `RAISE EXCEPTION` rolls back the attempt-log row along with everything else; relying on code entropy (~2^50 keyspace) + the successful-joins-per-hour cap instead).

**Verification:** `flutter analyze` — 208 issues repo-wide, all info-level, zero warnings/errors (only pre-existing-style infos, same tolerance already present elsewhere in this codebase). `flutter test` — **713/713 passing**, including 130 organization-feature tests (all new join-code coverage plus every pre-existing org test, run as one suite: `flutter test test/features/organization/`). `dart format lib/ test/` — clean. Not yet committed.

**Confirmed clean on closer inspection (not new fixes, just verified rather than assumed):** the stage31 RLS helper functions are correctly marked `stable`; the org/work-mode schema (stage28-30) is thoroughly indexed via implicit unique-constraint indexes on every lookup pattern actually used; `sync_post_like_count` was confirmed to be the *only* synchronous shared-counter hot-row pattern anywhere across all 34 migrations.

## Social feed gaps, part 1: unpublish/delete a post (2026-08-10)

First of three explicitly-deferred Stage 22 items (unpublish/delete, notifications, comments — see that stage's entry above), picked up at user request.

- [x] **New migration `docs/supabase_migrations/stage36_post_delete.sql`** — adds an author-only `delete` RLS policy to `itinerary_posts` (previously insert-only/immutable by design). No other schema change needed: `post_likes` already cascades on `post_id`, and both `itinerary_posts.forked_from_post_id` (self-FK) and `itineraries.origin_post_id` are already `on delete set null` (stage22), so deleting a post just silently drops "remixed from" lineage on any forks rather than blocking the delete or cascading through them. **✅ Run against the live database (2026-08-11).**
- [x] **Domain/data** — `SocialRepository.deletePost(postId)` + `DeletePostUseCase`, datasource/repository impl wiring (a plain `.delete().eq('id', postId)`, RLS does the author check).
- [x] **UI** — `PostCard` gained an optional `onDelete` callback (null hides the affordance entirely — caller decides ownership, never the widget). Wired into `DiscoverPage`'s Explore/Following tabs (shown only when `post.authorId == currentUserId`, i.e. your own post can surface in the public feed too) and `PublicProfilePage`'s own-posts list (`isOwnProfile` gate), both behind the same delete-confirmation `AlertDialog` pattern already used for delete-account.
- **Deliberate scope decision, not re-litigated from stage22:** deleting a post does not touch `TravelItinerary.isPublic` (the "has this ever been published" badge) — that flag is a one-way historical marker by design, same as editing an itinerary never un-publishes it.
- **Tests** — `delete_post_usecase_test.dart`, a `deletePost` group in `social_repository_impl_test.dart`, and two new `PostCard` widget tests (affordance hidden when `onDelete` is null, fires when tapped).

**Verification:** `flutter analyze` — 208 issues, all info-level (same baseline as stage35). `flutter test` — 719/719 passing (+8 from the 711 baseline — note the running total in this doc undercounts slightly since `setUpAll`/`tearDownAll` pseudo-tests are counted by `flutter test`'s own tally but not by hand here). `dart format lib/ test/` — clean.

## Social feed gaps, part 2: like/follow/new-post notifications (2026-08-10)

Second of the three deferred Stage 22 items. Full design/rationale writeup at `lib/features/notifications/CLAUDE.md`.

- [x] **New migration `docs/supabase_migrations/stage37_social_notifications.sql`** — `public.notifications` table (no insert policy at all — every row written by a `SECURITY DEFINER` trigger, never a client insert), 3 triggers (`notify_on_post_like`, `notify_on_follow`, `notify_on_new_post` — the last fans out to followers, capped at 1000 same as `fetchFeed`), and a new `social_activity` notification-preferences category (same widen-constraint/reissue-seed-function/backfill pattern stage19 used for `chat_messages`). **✅ Run against the live database (2026-08-10, after the deployment-blocker fix below).**
- [x] **New feature `lib/features/notifications/`** — full Clean Architecture layers; one bounded realtime `.stream()` watch (50 rows, newest-first) backs the feed page, the unread badge, and the foreground local-notification watcher — a deliberate scope trim vs. full keyset pagination (documented, not hidden).
- [x] **UI** — bell icon + unread badge on `DiscoverPage`'s `AppBar` → `/notifications`; `NotificationsPage` marks everything read on open. Every notification type routes to the actor's `/u/:actorId` — no single-post detail route exists anywhere in this app to route to instead.
- [x] **Push delivery** — new Edge Function `supabase/functions/send-social-push`, mirroring `send-message-push`'s Android-data-only/iOS-alert split; invoked best-effort from `SocialRemoteDataSourceImpl` after like/follow/publish succeed, re-deriving recipients server-side rather than trusting the client. Extended the shared push infra (`push_message_handler.dart`, `NotificationService`) with a `kind: 'chat' | 'social'` discriminator so one background handler and one local-notification tap callback cover both push kinds — `send-message-push` picked up `kind: 'chat'` too, no behavior change.
- **Real bug caught while testing, not by design review:** `NotificationsPage` wrote directly to a `StateProvider` from `initState`/`dispose` — Riverpod forbids modifying a provider mid-build, and a widget's `initState`/`dispose` both count. Fixed by deferring both writes (`initState`'s via `addPostFrameCallback`, `dispose`'s via `Future.microtask` with a `StateController.mounted` guard, since the container can already be gone by the time a disposal-time microtask runs) — same pattern `ChatPage`'s `activeChatIdProvider` handling already uses, just not one this codebase had a test catching until now.
- **Tests** — new `test/features/notifications/` (entity, model, repository, both usecases, a widget test suite for `NotificationsPage`), plus 2 new `handleIosPushTap` cases in `test/core/notifications/push_message_handler_test.dart` for the social branch.

**Verification:** `flutter analyze` — 212 issues, all info-level (new ones are `prefer_const_literals`/`avoid_redundant_argument_values` nits in the new test files, same tolerance as the rest of this codebase). `flutter test` — **738/738 passing**. `dart format lib/ test/` — clean.

## Social feed gaps, part 3: post comments (2026-08-10)

Third and last of the deferred Stage 22 items. Full design writeup extended in `lib/features/notifications/CLAUDE.md` (the notification side) — comments themselves live in `lib/features/social/` alongside likes/follows, not a separate feature, since they're exclusively about `itinerary_posts`.

- [x] **New migration `docs/supabase_migrations/stage38_post_comments.sql`** — `post_comments` table (read-all/insert-own/delete-own RLS, immutable — no update policy, matching `itinerary_posts`), a 30/min rate-limit trigger (same BEFORE INSERT pattern as stage33's likes/follows/posts limits), and a `notify_on_post_comment` trigger extending stage37's notifications table with a 4th type (`comment`). **Deliberately no stored `comment_count` column** — SCALE-001 already hit exactly this hot-row problem with `like_count` (stage33) and the fix was read-time counting instead; comments get that treatment from day one rather than repeating the mistake. **✅ Run against the live database (2026-08-10).**
- [x] **`ItineraryPost`/`ItineraryPostModel` gained `commentCount`**, read via the same PostgREST embedded-count trick as `likeCount` (`post_comments(count)` alongside the existing `post_likes(count)`) — `ItineraryPostModel._likeCountFromJson` generalised into `_countFromJson(json, embedKey)` since the two are now identical logic.
- [x] **Domain/data** — `PostComment` entity, 3 new `SocialRepository` methods (`watchComments`/`addComment`/`deleteComment`) + usecases, datasource wiring (`.stream()`-based watch, same re-sort-every-emission caveat as chat's `watchMessages`).
- [x] **UI** — `PostCard` gained a comment icon+count next to the like control; tapping it opens `CommentsSheet`, a `DraggableScrollableSheet` modal (not a new route — no single-post detail page exists anywhere in this app, and a sheet matches the existing bottom-sheet idiom used for the theme picker/avatar actions/segment actions/profile field pickers) with a live comment stream, an add-comment input, and delete on your own comments. Closing the sheet refreshes the underlying feed/profile post list so the card's `commentCount` catches up (the sheet's own list is a separate live stream that doesn't otherwise feed back into the card).
- [x] **Push + in-app notifications** — extended stage37's `send-social-push` and the `notifications` table/`NotificationType` enum with a `comment` case end-to-end (Edge Function, trigger, `notificationText()`, `NotificationsPage` icon).
- **Tests** — new `post_comment_model_test.dart`, 3 new usecase test files, a `watchComments`/`addComment`/`deleteComment` group in `social_repository_impl_test.dart`, `PostCard` comment-affordance tests, and a full `comments_sheet_test.dart` (stream rendering, empty state, own-comment-only delete affordance, send-and-clear).

**Verification:** `flutter analyze` — 221 issues, all info-level (new ones are test-file nits, same tolerance). `flutter test` — **759/759 passing**. `dart format lib/ test/` — clean. Not yet committed.

All three explicitly-deferred Stage 22 social-feed items (unpublish/delete, notifications, comments) are now closed. Migrations stage36–38 are now all live (see later entries — stage36 confirmed 2026-08-11, stage37–38 confirmed below).

## SCALE-014 fix: bound PostFeedNotifier's "Load more" memory growth (2026-08-10)

`docs/SCALABILITY_AUDIT.md` had explicitly left this "documented, not fixed" as a product/UX trade-off rather than deciding unilaterally. Asked the user directly (efficiency vs. UX impact of capping); they chose to cap it.

- [x] Added `kMaxFeedWindowPosts` (10 pages / 200 posts) to `lib/features/social/presentation/providers/social_provider.dart` — `PostFeedNotifier.loadMore()` now evicts the earliest-loaded page from the front once the concatenated list exceeds that cap. The pagination cursor (`current.posts.last.createdAt`) always derives from the tail, so trimming the front never disturbs it.
- **Trade-off, chosen not assumed:** scrolling back up past the evicted window now shows a fresh reload from the top instead of the exact posts scrolled past earlier. Only reachable after 10+ "Load more" taps in one session.
- **Tests** — new `loadMore evicts the oldest page once kMaxFeedWindowPosts is exceeded` case in `social_provider_test.dart`, driving 11 consecutive pages through a real `PostFeedNotifier` and asserting both the cap and that the pagination cursor still advances correctly post-eviction.
- Marked ✅ Fixed in `docs/SCALABILITY_AUDIT.md`'s SCALE-014 entry and the Verdict summary line.

**Verification:** `flutter analyze` — 222 issues, all info-level. `flutter test` — **760/760 passing**. `dart format lib/ test/` — clean.

## Join-code deep link: kumo://join?code=XYZ (2026-08-10)

One of two follow-ups explicitly flagged as deferred in the stage35 join-code work (the other, per-department feature-flag/approval-routing overrides, is separate). Scan-free alternative for a code shared by email/Slack — the QR/manual-entry flow (`JoinOrganizationPage`) remains the primary UX.

- [x] Registered the `kumo://` custom URL scheme on both platforms — Android `AndroidManifest.xml` intent-filter (`scheme="kumo" host="join"`), iOS `Info.plist` `CFBundleURLTypes` (no `AppDelegate.swift` change needed; `FlutterDeepLinkingEnabled` defaults to true).
- [x] Handled in `lib/config/router.dart`'s top-level `redirect()`, not a plain `GoRoute` path pattern — a custom-scheme URI's authority ends up as `state.uri.host`, not `.path`, so a path-pattern route can't match it directly; both are checked defensively since which shape the platform actually delivers isn't something this environment can verify (no device/simulator here).
- [x] `JoinOrganizationPage` gained an `initialCode` param — pre-fills the manual-entry field and defaults to that view instead of the camera (skips an unnecessary camera-permission prompt too, since the scanner widget is never built when a code already arrived via link).
- **Deliberate scope trim:** only handled when already signed in. An unauthenticated tap falls through to the normal `/login` gate and the code is lost — re-entering/re-scanning after login is the fallback. Preserving it across the auth gate cleanly needs either a provider write from inside `redirect()` (untested — same reasoning as above) or threading a query param through every intermediate redirect hop (onboarding, etc.); not attempted this pass.
- **Explicit, acknowledged gap:** this whole path — the native manifest/plist registration and the redirect's `state.uri.host`-vs-`.path` handling — has had no real-device or simulator smoke test, since none is available in this environment. Recommend one manual QA pass (tap a `kumo://join?code=XXX` link on both a real Android and iOS build) before relying on this in production.
- **Tests** — `test/features/organization/presentation/pages/join_organization_page_test.dart` (new — first widget-test coverage for this feature's presentation layer): `initialCode` prefill + default view, and that the code reaches `redeemOrgJoinCodeUseCaseProvider`. The router's `redirect()` itself isn't covered — `_RouterNotifier` is private/unexported and this app has no router tests at all yet, not just for this feature.

**Verification:** `flutter analyze` — 222 issues, all info-level. `flutter test` — **763/763 passing**. `dart format lib/ test/` — clean.

## Per-department approval-routing thresholds + feature-flag overrides (2026-08-10)

Second of the two stage35 join-code follow-ups (the other, the deep link above, is stage38's sibling). Genuinely different in kind from everything else this session — no existing pattern to extend, so scope was confirmed with the user directly before building: (1) does a threshold auto-approve or just flag-for-review — **auto-approve**, and (2) what does a feature override actually grant — **a premium-gating exception**, both per department (`org_cost_field_options`, i.e. whatever field an org calls "Department").

- [x] **New migration `docs/supabase_migrations/stage39_department_overrides.sql`**:
  - `organizations.default_approval_threshold` (org-wide) + `org_cost_field_options.approval_threshold` (department override, takes precedence) + `expenses.auto_approved`.
  - Auto-approval logic folded into the *existing* `guard_expense_review_fields` trigger (stage29) rather than a new separate trigger — Postgres fires same-table `BEFORE UPDATE` triggers in alphabetical-by-name order, and a second trigger would've hit an ordering hazard: it'd run before the admin-only guard (alphabetically), handing it an already-`'approved'` row with no way to distinguish "the system just auto-approved this" from "a non-admin payer tried to self-approve" — exactly what that guard exists to block. One function sidesteps it.
  - `auto_approved` is fully server-computed inside that same function, every time, regardless of what a client sends — `expenses_payer_update` (stage29) lets a payer update any column on their own row, so without unconditionally overwriting it, a payer could spoof `auto_approved = true` to slip past the admin check without actually qualifying.
  - `set_org_default_approval_threshold` RPC (org has no admin-scoped update RLS, owner-only — an RPC avoids widening that). The department-level threshold needs no RPC — `org_cost_field_options_admin_write` (stage31) already grants admins full row control.
  - New `org_feature_overrides` table, grant-only (never a deny) — layered on top of, never replacing, the existing per-user `isPremiumUserProvider` check. `has_department_feature_override` RPC for the narrow "does my department grant me this" read (no member-select RLS on the table itself — only admins need to browse the whole thing, to manage it).
  - **✅ Run against the live database (2026-08-11).**
- [x] **Domain/data** — `Organization.defaultApprovalThreshold`, `OrgCostFieldOption.approvalThreshold`, new `OrgFeatureOverride` entity; 4 new `OrganizationRepository` methods + usecases; `PremiumFeatureDataSource.hasDepartmentOverride` + `departmentFeatureOverrideProvider`; `canUseFeatureProvider` now checks premium status OR department override (either sufficient).
- [x] **UI** — `OrgCostFieldsSettingsPage` gained a leading org-default-threshold card and a per-department settings sheet (threshold + a switch per premium feature) reached via a new icon next to each department's existing delete action.
- **Deliberate scope trim, not surfaced:** `expenses.auto_approved` is stored but not read anywhere in the Flutter `Expense` entity/UI — no existing UI in this app distinguishes *how* an expense got approved (there's no approval-status display at all outside the admin queue, and auto-approved expenses never enter that queue in the first place), so there was nothing to wire it into without inventing new UI beyond what was asked.
- **Tests** — closed a real pre-existing gap while here: `lib/core/premium/` had **zero** test coverage before this (not something this change caused, just first contact with untested code, same as several earlier stages' audit passes). New `test/core/premium/` (2 files) covers `PremiumFeature.isGated` and `canUseFeatureProvider`'s full resolution chain including the new department-override branch. Also new: 2 entity, 3 model, 4 usecase test files, and a `organization_repository_impl_test.dart` extension — all for the org side.

**Verification:** `flutter analyze` — 229 issues, all info-level (new ones are test-file nits, same tolerance as the rest of this codebase). `flutter test` — **798/798 passing**. `dart format lib/ test/` — clean. Not yet committed.

Both stage35 follow-ups (deep link, department overrides) are now closed. stage37 and stage38 are now live (see the deployment-blocker note below); stage36 and stage39 are also now live (2026-08-11).

## stage37 deployment blocker: pre-existing public.notifications table (2026-08-10)

Running `stage37_social_notifications.sql` against the live database failed — `column "recipient_id" does not exist`, then (on a later attempt) `invalid input value for enum notif_type`. The live project already had a `public.notifications` table that predates this repo's migration history entirely (no migration in `docs/supabase_migrations/` creates one before stage37) — some `type` column typed as an enum (`notif_type`) with no `recipient_id`, origin unknown. `create table if not exists` silently no-opped against it, leaving every later statement in the file operating against the wrong schema.

- [x] **Fixed in `stage37_social_notifications.sql` itself** — a guard block at the top detects a pre-existing `public.notifications` missing a `recipient_id` column and renames it to `notifications_legacy_<timestamp>` (never drops it — whatever's in there, if anything, is preserved) before the real table is created. Re-run-safe: once the correct table exists, the guard is a no-op.
- [x] **Confirmed working** — stage37 and stage38 both ran clean against the live database after this fix.
- **Still open, not urgent:** never got a definitive read on what the pre-existing table actually was (columns, row count) before the fix went in — diagnostic queries kept coming back as the *original* stage37 error instead of their own results, so the rename-and-preserve approach was applied blind rather than confirmed against a known schema. Whatever it was is now sitting under a timestamped `notifications_legacy_*` name in the live database rather than being lost — worth a manual look (`select * from public.notifications_legacy_...`) next time you're in the SQL editor, purely out of curiosity/cleanup, not because anything depends on it.

## Scalability audit: closing out the remaining items (2026-08-11)

- [x] **stage34** (SCALE-013's fix, consolidating `itinerary_posts`' two redundant rate-limit triggers into one) — **✅ run against the live database.**
- [x] **SCALE-012** (Nominatim geocoding / OSRM routing are both free demo-tier services, no production SLA) — asked directly: swap to a paid provider, self-host OSRM, or accept as-is. **Decision: accept as-is** — both already degrade gracefully (geocoding throttles rather than erroring; routing falls back to a straight/curved line with no user-facing error), so there's no live bug being accepted, just a known reliability ceiling. Revisit only if the rate limit is actually hit in practice.
- **Not touched, deliberately:** SCALE-002 (queue infra), SCALE-006 (JSONB-array RLS scans), SCALE-007 (synchronous push fan-out), SCALE-010 (Realtime connection ceiling), SCALE-011 (single Postgres instance, read replicas). All five are real-infrastructure or platform-level items the audit's own verdict already argues against building preemptively — "not a live problem at this app's actual data shapes," not gaps found and skipped this pass.

Every SCALABILITY_AUDIT.md finding is now either fixed-and-live, explicitly decided, or documented as correctly not-yet-justified. Migrations stage36 and stage39 were the only ones outstanding against the live database at the time — **both now run (2026-08-11); every migration through stage39 is live.**

## Test coverage pass (2026-08-11)

Re-ran the missing-unit-test audit style used earlier this project (grep which classes are actually exercised in `test/`, not just filename matching — the filename heuristic has false positives, e.g. several "missing" usecase tests turned out to already be covered by combined delegator-usecase test files). Found and closed real gaps, all zero-coverage before this pass:

- [x] **`UserProfileRepositoryImpl`** — zero coverage despite backing every profile read/write in the app. New `test/features/profile/data/repositories/user_profile_repository_impl_test.dart`, 16 tests across all 5 methods (success path + each exception-to-failure mapping).
- [x] **`NotificationPreferenceModel`** — zero coverage of `fromJson`. New `test/features/profile/data/models/notification_preference_model_test.dart`.
- [x] **`UserModel`** — zero coverage of `fromSupabaseUser`/`fromJson`/`toJson`, despite being the model every login/signup resolves through. New `test/features/auth/data/models/user_model_test.dart`, including the `sb.User` → `UserModel` field mapping and its several defaulting rules (empty email, `emailVerified` false when unconfirmed, etc.).
- [x] **`AuthNotifier`** — zero dedicated test despite being exercised as a fixture-builder in a dozen other test files (which only ever stub `getCurrentUser`, never assert its own state-machine behavior). New `test/features/auth/presentation/providers/auth_provider_test.dart`, 17 tests covering every method's state transitions: construction, `login`/`signup`/`logout` success+failure, `updatePassword` (success logs out, failure doesn't), `updateProfile`/`deleteAccount` (state only changes on success, return value always reflects the result), `clearError`.
- [x] **`OnboardingNotifier`** — zero coverage of its per-user-scoped completion tracking. New `test/features/onboarding/presentation/providers/onboarding_provider_test.dart`, 7 tests including the two-user switch-and-resync case (signed in as user-1, log in as user-2 mid-test, confirm each user's own stored completion state is read independently) — this is the one piece of real logic in the file (`_lastUserId` caching) and the part most likely to have a copy-paste-across-users bug if it ever regressed.

**Verification:** `flutter analyze` — 249 issues, all info-level (new ones are test-file nits, same tolerance as the rest of this codebase). `flutter test` — **846/846 passing** (+48 from the 798 baseline above). `dart format lib/ test/` — clean. Not yet committed.

## Work Mode / Private Mode toggle (2026-08-11)

Requested feature, not audit-driven: a global mode switch for users who both work for an org on Kumo and travel personally (e.g. a consultant) — separates the two contexts visually (a forced dark "Onyx & Gold" theme + a "Kumo — for {Org}" banner while in Work Mode) and functionally (Home/Trips filters to the current context; trip creation auto-tags instead of showing a manual org picker). Invisible to the majority of users who belong to zero orgs. Scope confirmed directly with the user: at most one org per user (no picker for the theoretical multi-org edge case — falls back to the first), and Work Mode's trip visibility shows only the signed-in user's own trips at that org (owned or member-of) — never an admin-oversight view of teammates' trips; per the user, any future admin-side workflow belongs to a future admin portal or an external system (e.g. SAP), not this app. (A 2026-08-11 audit pass found the code comment originally justifying this had gone stale — see that entry below.)

- [x] **New `lib/features/work_mode/` feature** — `WorkModeNotifier` (per-user-scoped `SharedPreferences` persistence, structural mirror of `OnboardingNotifier`), `workModeProvider` (raw persisted choice), `isWorkModeAvailableProvider` (has ≥1 org), `isWorkModeActiveProvider` (the one every other consumer reads — self-heals if org membership disappears mid-session while the persisted flag is still `true`), `currentWorkOrgProvider`. `WorkModeBanner` (three-state: hidden / "Personal" + switch / "Kumo — for {Org}" + switch), wired into `KumoShell` above `OfflineBanner`.
- [x] **"Onyx & Gold" theme** — new `KumoTheme.onyxGold` enum value (dark, metallic gold/bronze/copper palette), added to `lib/config/theme.dart` and `Brand.logoFor()` (reuses the pre-existing, previously-unused `kumo_logo_stacked_premium_gold.svg` asset — same viewBox as every other theme's logo). **Enforced-only**: excluded from the theme picker's `KumoTheme.values` list, never reachable via `setTheme()`. New `effectiveThemeProvider` (forces `onyxGold` while Work Mode is active, otherwise passes through the user's own persisted `themeProvider` choice — Work Mode never writes to `themeProvider`, so leaving it always restores the exact prior personal theme) is what `main.dart` now renders from instead of raw `themeProvider`.
- [x] **Trip filtering** — new `visibleItinerariesProvider` in `itinerary_provider.dart`: Private Mode shows only `orgId == null` trips; Work Mode shows only the current org's trips the user owns or is a member of (explicitly re-filters out the admin-oversight-visible-but-not-owned rows RLS would otherwise return). `home_page.dart` and `trips_page.dart` both swapped to consume it instead of the raw itinerary list.
- [x] **`CreateItineraryPage` simplified** — deleted the manual `_OrgPicker` dropdown entirely; `orgId` is now auto-derived from Work Mode state at submit time. `CostFieldPicker` visibility changed from "org selected" (opt-in) to "Work Mode active" (automatic) — a real behavior change: cost-tracking tagging is no longer optional once in Work Mode. Added a one-line "This trip will be tagged to {org.name}" notice so that's never a surprise.
- [x] **`profile_page.dart` gating** — "My Organizations" tile now shown only while Work Mode is active (incidentally fixes a pre-existing bug: it was previously shown unconditionally, even to zero-org users). Appearance tile locks to a non-interactive "Theme: Onyx & Gold (locked by Work Mode)" row while active, with a snackbar explaining why, instead of adding a themepicker-metadata entry for a theme that's never user-selectable anyway.
- **Not built, deliberately:** no admin-oversight view anywhere in-app (per the scope decision above) — Work Mode is a user-end integration only, not a management surface.
- **Tests** — new `work_mode_provider_test.dart` (14 tests, mirroring `onboarding_provider_test.dart`'s real-`AuthNotifier`-via-mocked-usecases approach), `effective_theme_provider_test.dart` (3 tests, explicitly asserting the "restores exact prior theme" guarantee), `visible_itineraries_provider_test.dart` (2 tests, locking in the ownership/membership restriction as the thing most likely to silently regress), `work_mode_banner_test.dart` (3 widget tests), and net-new `create_itinerary_page_test.dart` (5 widget tests — this page had zero prior coverage — covering picker removal, auto-tag notice/cost-field visibility, and that submitted `orgId` matches the mode via a full form-fill-and-submit flow through a real `GoRouter`).

**Verification:** `flutter analyze` — 253 issues, all info-level (same tolerance as the rest of this codebase). `flutter test` — **873/873 passing** (+27 from the 846 baseline above). `dart format lib/ test/` — clean. No device/simulator available in this environment, so the actual retheme/banner/filtering behavior hasn't been visually smoke-tested — flagging as unverified, consistent with how the join-code deep link was handled. Not yet committed.

## Audit round: security + scalability + SOLID + Work Mode review (2026-08-11)

Requested explicitly ("do another round of audit"), scoped to (a) the Work Mode feature just built, checked against all three tracked audit dimensions, plus (b) a fresh functional review of that same feature — not a full re-audit of the entire codebase from scratch in each category, matching the "second pass" methodology `SCALABILITY_AUDIT.md` already established (recheck new/changed code, confirm nothing regressed, don't reproduce work already done).

- [x] **Security — verified, not assumed:** confirmed `itineraries_owner_all` (`stage31_fix_org_members_rls_recursion.sql:168-173`) actually rejects an INSERT/UPDATE whose `org_id` doesn't match an org the caller belongs to (`with check (owner_id = auth.uid() and (org_id is null or is_org_member(org_id)))`) — `CreateItineraryPage` now derives `org_id` from client state rather than a picker, so this was the one place client-trusted input meets a real permission boundary, and it holds. No new SEC-XXX finding; see `docs/SECURITY_AUDIT.md`'s 2026-08-11 note.
- [x] **Security — caught a stale reference in code I'd just written:** `visibleItinerariesProvider`'s doc comment (and the Work Mode Checklist entry above) cited `org_admin_trip_visibility_select` as a still-active RLS grant the client-side filter needed to defend against. It was actually dropped in SEC-029 (2026-08-09, *before* Work Mode was written) and replaced by the narrow `fetch_org_pending_approvals` RPC. The client-side filter itself was never wrong (harmless defense-in-depth, matches the confirmed product scope) — only its stated justification was outdated. Fixed both the code comment and the earlier Checklist entry to describe the real, current rationale.
- [x] **Real bug found and fixed — `profile_page.dart`'s "My Organizations" tile:** was gated on `isWorkModeActiveProvider` (current mode) instead of `isWorkModeAvailableProvider` (has an org, any mode) — meaning a user with an org but currently in Private Mode couldn't reach org management at all (join codes, member list, cost-field settings) without first flipping into Work Mode and retheming their whole app. The zero-org-user fix from the original implementation was correct; it just used the wrong provider. Fixed to gate on availability, not activity. New `test/features/shell/profile_page_test.dart` (5 tests) locks in both the org-tile gating (hidden/zero-org, visible/inactive, visible/active) and the Appearance-tile lock behavior — first coverage this page has ever had.
- [x] **Scalability:** reviewed — no new findings. `visibleItinerariesProvider` is pure in-memory filtering over data already fetched for other reasons; `WorkModeBanner` reads only already-cached provider state. See `docs/SCALABILITY_AUDIT.md`'s third-pass note.
- [x] **SOLID:** reviewed — no domain-layer/DIP violations (presentation-only, mirrors `OnboardingNotifier`'s established shape). Two things recorded, neither fixed here: `WorkModeNotifier` structurally duplicates `OnboardingNotifier` (candidate for a shared base, folded into that audit's existing dedup priority item); adding `onyxGold` confirmed — rather than found new — the existing "KumoTheme → visuals, 4 files" OCP finding. See `docs/SOLID_AUDIT.md`'s 2026-08-11 addendum.
- **Documented, not fixed — a narrow transient race in `CreateItineraryPage`:** if `myOrganizationsProvider` is still resolving at the moment of submit (e.g. a user navigates straight to create-trip right after login, before orgs have loaded), `isWorkModeActiveProvider` reads as `false` during that window regardless of the user's actual persisted preference, so `effectiveOrgId` comes out `null` and the trip is silently created as personal. Self-corrects on the next attempt once orgs finish loading (typically sub-second, and only reachable in that one narrow startup window) — low severity, not fixed given how narrow and self-correcting it is, but worth knowing about if a work-mode trip ever shows up untagged.
- Also fixed a `Checklist.md` structural slip from the previous "Test coverage pass" entry — its closing verification line had ended up after the Work Mode section instead of terminating its own section.

**Verification:** `flutter analyze` — 253 issues, all info-level (unchanged — the `profile_page.dart` fix and new test file are both clean). `flutter test` — **878/878 passing** (+5 from the 873 baseline above, all from the new `profile_page_test.dart`). `dart format lib/ test/` — clean. Not yet committed.

## Migration deployment status: all caught up (2026-08-11)

User confirmed every outstanding SQL migration has now been run against the live database — closing out `stage36_post_delete.sql` and `stage39_department_overrides.sql` (the last two open), and also `stage23_security_hardening.sql`, which `docs/SECURITY_AUDIT.md` had been flagging as un-deployed since 2026-08-05. Every migration through stage39 is now confirmed live. Updated the running status notes above, `docs/SECURITY_AUDIT.md`'s executive summary, and `docs/DEVELOPMENT_ROADMAP.md`'s outstanding-migrations rows accordingly.

**Still outstanding (not migrations — separate deployment steps):** Edge Function redeploys + `ALLOWED_ORIGIN` secret (SEC-002/SECURITY_AUDIT.md), the `--dart-define-from-file` build switch, Katha AI / push-notification secrets + function deploys, a real Google Maps API key, and the masked-email inbound-provider wiring — see `docs/DEVELOPMENT_ROADMAP.md`'s Outstanding list for the full set.

## SOLID audit cleanup, round 1 (2026-08-11)

Continuing development with no new feature request pending — worked through `docs/SOLID_AUDIT.md`'s ranked priority list, starting with the two cheapest/highest-value items: the one genuine domain-layer framework leak, and the one violation flagged as a real correctness risk rather than just style.

- [x] **Priority #1 — `TripTheme.withContext()` domain-layer leak.** `lib/features/itinerary/domain/entities/trip_theme.dart` imported all of `flutter/material.dart` and had a `BuildContext`-dependent method, contradicting this project's own CLAUDE.md ("no dependencies on data layer, presentation, or external libraries"). Moved `withContext` to a new `TripThemeContextX` extension in `lib/features/itinerary/presentation/extensions/trip_theme_context_extension.dart`; narrowed the entity's remaining import to `package:flutter/painting.dart show Color, LinearGradient` (the plain paint-value types its other getters need — deliberately not touched, since the audit's own finding was scoped to the `BuildContext` coupling, not these). Updated the 3 call sites (`post_card.dart`, `itinerary_detail_page.dart`, `itinerary_card.dart`). New `test/features/itinerary/presentation/extensions/trip_theme_context_extension_test.dart` (2 tests) — first coverage `withContext` has ever had.
- [x] **Priority #5 — duplicated expense-summary recalculation math.** `add_expense_page.dart`'s add path and `itinerary_detail_page.dart`'s delete path each hand-rolled the same category-map/total mutation independently. Unified behind a new `ExpenseSummary.adjustedBy({categoryKey, delta})` domain-entity method (a positive delta for add, negative for delete) — both call sites are now one-line delegations. **Found a real latent inconsistency while unifying, not just deduplicating style:** the add path had no non-negative clamping at all; only the delete path did. Both now share the same clamp. New `test/features/itinerary/domain/entities/expense_summary_test.dart` (5 tests) covering the add/remove/clamp/no-prior-spend/no-mutation cases.
- Updated `docs/SOLID_AUDIT.md`'s SRP table row, cross-cutting-themes note, and ranked priority list to mark both items ✅ Fixed.
- **Remaining on the list, not attempted this pass:** splitting `SocialRepository` (#2), Auth's mixed DIP story (#3), `InviteMemberPage`/`PrivacySettingsPage` bypassing usecases (#4), and 8 more — see `docs/SOLID_AUDIT.md` for the full ranked list.

**Verification:** `flutter analyze` — 254 issues, all info-level (+1 over the 253 baseline: a single `cascade_invocations` nit in the new expense-summary test, same tolerance as elsewhere). `flutter test` — **885/885 passing** (+7 from the 878 baseline above). `dart format lib/ test/` — clean. Not yet committed.

## Gamification: XP, levels, and badges (2026-08-11)

User chose this over continuing the SOLID cleanup list — the only item on `docs/DEVELOPMENT_ROADMAP.md`'s "What Remains" table not blocked by anything external (unlike Stripe/full-B2B-portal/Concierge-AI). Planned via Explore research into the notification-trigger pattern, the `profile_status` append-only-ledger pattern, and trip-lifecycle hook points before writing any code.

- [x] **New migration `docs/supabase_migrations/stage40_gamification.sql`** — `xp_events` append-only ledger table (same shape as `profile_status`), RLS with **no insert policy at all** (every row written by a `SECURITY DEFINER` trigger, same anti-spoofing posture as `notifications`), and 6 trigger functions awarding XP on: a trip being created (+10), a trip's status transitioning to `completed` (+30), a post being published (+15), a post being liked (+2, to the author), gaining a follower (+5), and posting a comment (+3). **✅ Run against the live database (2026-08-11).**
- [x] **Anti-cheat via a unique dedup index, not a rate-limit trigger.** `xp_events_dedup_idx` on `(user_id, source_type, source_id)` + `on conflict ... do nothing` in every trigger. `source_id` is the source row's own primary key for one-shot events (closes "toggle a trip's status back and forth to farm XP"), or a `<target>:<actor>` pair for likes/follows (closes "unlike/relike, unfollow/refollow to farm XP" while still letting a *different* liker/follower award separately).
- [x] **Zero changes to any existing mutation code path.** Creating a trip, publishing a post, liking, following, and commenting are all untouched — XP is awarded entirely server-side by triggers on tables that already exist, exactly like `public.notifications`. The whole feature is new, read-only Flutter code on top.
- [x] **Domain/data** — `XpEvent`/`XpSummary`/`GamificationBadge` entities (`GamificationBadge`, not bare `Badge`, to avoid colliding with `flutter/material.dart`'s own `Badge` widget), `GamificationRepository`/`FetchXpEventsUseCase`. `XpSummary.fromEvents()` and all 8 `GamificationBadge.all` presets (metric + threshold, no closures) are pure client-side computation — no `badges`/`user_badges` table exists server-side; "earned" is derived fresh every time from the same ledger the level/total already come from.
- [x] **Presentation** — `xpEventsProvider`/`xpSummaryProvider` (both `.autoDispose` — one consumer surface, re-fetch fresh on each visit rather than wiring `ref.invalidate` into 6 scattered award call sites; no realtime). `BadgeUnlockNotifier` (structural mirror of `OnboardingNotifier`/`WorkModeNotifier` — per-user `SharedPreferences` seen-set) + `GamificationCard` (new, inserted into `profile_page.dart` after `_StatsCard`) using `ref.listen` — not a `postFrameCallback` — to trigger `checkForNewBadges` and a celebratory dialog the moment a fresh summary crosses a new threshold. New `AchievementsPage` (`/achievements`): level/XP header, the full 8-badge grid (locked badges greyed out, threshold in a tooltip), and the 10 most recent XP events.
- **Deliberate scope trims:** expenses excluded as an XP source (no rate limit anywhere on that table today, trivially farmable by spamming tiny expenses — revisit only if that changes for other reasons); no push notification or persisted `notifications` row for a badge unlock (in-app celebration only, to avoid touching the `notifications` table/`NotificationType` enum/`send-social-push` for this pass).
- [x] **New `lib/features/gamification/CLAUDE.md`** — full schema/trigger/design-decisions writeup, same skeleton as `lib/features/notifications/CLAUDE.md`.
- **Tests** — 34 new tests: `xp_summary_test.dart` (aggregation + level arithmetic at boundaries), `gamification_badge_test.dart` (every badge's threshold boundary, parameterized over all 8), `xp_event_model_test.dart`, `gamification_repository_impl_test.dart`, `fetch_xp_events_usecase_test.dart`, `gamification_provider_test.dart`, `badge_unlock_notifier_test.dart` (mirrors `onboarding_provider_test.dart`'s per-user-persistence/independent-users/reset-on-sign-out coverage), `achievements_page_test.dart`.

**Verification:** `flutter analyze` — 260 issues, all info-level (same tolerance as the rest of this codebase). `flutter test` — **919/919 passing** (+34 from the 885 baseline above). `dart format lib/ test/` — clean. No device/simulator available in this environment, so the card/dialog/grid rendering hasn't been visually smoke-tested — flagging as unverified, consistent with Work Mode and the join-code deep link. Not yet committed.

**Migration confirmed live (2026-08-11):** user ran `stage40_gamification.sql` against the live database — XP now actually accrues in production. Updated the running status notes above and `docs/DEVELOPMENT_ROADMAP.md`'s Stage 23 entry/Quality Gates/Outstanding list accordingly (`lib/features/gamification/CLAUDE.md`'s migration line is generic deploy instructions, not a live-status tracker, so it didn't need a change — same convention every other feature `CLAUDE.md` already follows).

## Audit round: gamification review + widened DIP finding (2026-08-11)

Requested explicitly ("another complete audit... fix any pending changes"), scoped to holding the newest feature (gamification, Stage 23) to the same anti-abuse and architecture standards already established elsewhere — same "second pass" methodology as the previous audit round, not a from-scratch re-derivation of all three tracked audits.

- [x] **Security — real gap found and fixed (SEC-032).** `award_xp_on_trip_created`/`award_xp_on_trip_completed` had no rate limit at all, unlike every other XP source. Stage 40's own migration comment had already articulated the right reasoning — "expenses have no rate limit anywhere, trivially farmable, exclude them" — but didn't apply it consistently to `itineraries`, which also has no INSERT rate limit anywhere in this schema (confirmed by grep across every migration). New migration `docs/supabase_migrations/stage41_gamification_rate_limits.sql`: throttles the XP *award* to 5/hour per source (independent counters for creation vs. completion) via `CREATE OR REPLACE` on the same two trigger functions — deliberately does not add a rate limit to `itineraries` itself, so trip creation/completion stays fully unrestricted for every user regardless of this feature. **✅ Run against the live database (2026-08-11).**
- [x] **SOLID — my own new code repeated an already-documented anti-pattern.** `gamificationRepositoryProvider` was typed `Provider<GamificationRepositoryImpl>` (concrete), not the abstract `GamificationRepository` — the exact DIP violation `docs/SOLID_AUDIT.md` already flags via `packing_provider.dart`/`rating_provider.dart`. Fixed before commit.
- [x] **SOLID — that finding's scope was undercounted; corrected it.** A repo-wide grep for the pattern found **11 repository providers** typed to their concrete `Impl` class, not the 2 originally documented — also `organization`, `chat`, `itinerary`, `trip_email_alias`, `trip_segment`, `ai_generation`, and `expense`, on top of `packing`/`ratings`/`auth`. Updated `docs/SOLID_AUDIT.md`'s finding text and ranked priority #9 to reflect the real scope (now sized like a real ~10-file pass, not a quick two-file cleanup) — **not attempted in this pass**, beyond fixing gamification's own instance.
- [x] **Scalability — reviewed, no new finding.** The rate-limit check adds one more indexed-prefix query per trip create/complete (`user_id` + time-window narrows via the existing `xp_events_user_id_created_at_idx`, `source_type` filtered as a cheap residual predicate over an already-small per-user result set) — not worth a dedicated index at this table's expected volume.
- Updated `lib/features/gamification/CLAUDE.md`'s schema and design-decisions sections to describe the rate limit.

**Verification:** `flutter analyze` — 260 issues, unchanged (the DIP fix is a one-line type-annotation change; the rate-limit migration is SQL-only, no Dart touched). `flutter test` — **919/919 passing**, unchanged. `dart format lib/ test/` — clean. Committed as `71f1495`.

## Test coverage pass, round 3 (2026-08-11)

Requested ("check pending unit tests"), same audit methodology as the two earlier passes this project — grep which classes are actually exercised in `test/`, not just filename matching (the filename heuristic flags false positives: 7 usecases initially looked untested but turned out to already be covered by combined delegator files — `packing_write_usecases_test.dart` and `fetch_social_reads_usecase_test.dart`/`social_provider_test.dart` — confirmed by grepping the class names inside those files before treating anything as a real gap). Found and closed 6 real gaps, all zero-coverage before this pass:

- [x] **`ItineraryListNotifier`** — the highest-value gap: backs both Home and Trips pages, 4 methods, zero dedicated test (the one incidental hit in `visible_itineraries_provider_test.dart` only used it as setup machinery, never exercised its own success/failure/state-transition behavior). New `test/features/itinerary/presentation/providers/itinerary_list_notifier_test.dart`, 12 tests: `loadItineraries`/`softRefresh`'s different failure-handling philosophies (error state vs. silently keep showing stale data), `createItinerary`'s prepend-to-existing-vs-start-fresh branches, `deleteItinerary`.
- [x] **`ThemeNotifier`** — the app's main theme system, zero direct test despite `effective_theme_provider_test.dart` existing (that file only ever seeds `SharedPreferences` and lets `_loadSaved()` passively pick it up — never calls `setTheme`/`toggle` itself). New `test/config/theme_provider_test.dart`, 5 tests including the unrecognised-stored-value fallback and `toggle`'s wraparound at the last enum value.
- [x] **`MapProviderNotifier`** — same shape/gap as `ThemeNotifier`, completely untested. New `test/core/maps/kumo_map_provider_test.dart`, 4 tests.
- [x] **`AiGenerationNotifier`** — zero coverage; the only test file in that presentation/providers directory (`resolve_ai_segments_test.dart`) covers a different class in the same source file. New `test/features/ai_generation/presentation/providers/ai_generation_notifier_test.dart`, 5 tests.
- [x] **`PackingItemModel`** — `fromJson`/`toJson` never exercised; the one repository test that references the class only ever uses the constructor directly as a fixture. New `test/features/packing/data/models/packing_item_model_test.dart`, 4 tests including the `isChecked`-defaults-false and optional-`category` handling.
- [x] **`TripCostFieldValueModel`** — same "constructor-as-fixture-only" gap, trivial model (3 plain string fields). New `test/features/itinerary/data/models/trip_cost_field_value_model_test.dart`, 1 test. (`TripEmailAliasModel`, initially flagged by the same filename heuristic, has no `fromJson`/`toJson` at all — nothing model-specific to test; not a real gap.)

**Verification:** `flutter analyze` — 265 issues, all info-level (+5 over the 260 baseline: new test-file nits, same tolerance as elsewhere). `flutter test` — **950/950 passing** (+31 from the 919 baseline above). `dart format lib/ test/` — clean. Committed as `04c5ae0`.

**Migration confirmed live (2026-08-11):** user ran `stage41_gamification_rate_limits.sql` against the live database — SEC-032's fix (rate-limited trip creation/completion XP) is now active in production. Updated the running status notes above and `docs/DEVELOPMENT_ROADMAP.md`'s Stage 23 entry/Quality Gates accordingly. Every migration through stage41 is now confirmed live.

## SOLID audit cleanup, round 2 (2026-08-11)

Continuing through `docs/SOLID_AUDIT.md`'s ranked priority list after the full doc re-parse confirmed rounds 1 and gamification were both fully closed out. Two more items:

- [x] **Priority #9 — 9 of the 11 concrete-typed repository providers retyped** to their abstract interface: `packing`, `ratings`, `organization`, `chat`, `itinerary`, `trip_email_alias`, `trip_segment`, `ai_generation`, `expense` (`gamification` was already fixed when found during its own audit round; `auth` is covered separately by priority #3, since fixing it is more than a type annotation — `AuthNotifier` calls the concrete repository directly for several methods). Found one genuinely redundant provider while doing it: `chatRepositoryRefProvider` was a second `Provider<ChatRepositoryImpl>` that only existed because `fetchMessagesBefore` apparently wasn't on the abstract `ChatRepository` at some point — it already is now, so the provider was pure duplication and got deleted; `chat_page.dart` reads `chatRepositoryProvider` directly instead.
- [x] **Priority #10 — `AppConstants`/`Validators`/`Formatters` grab-bag cleanup.** Deleted 6 stale `AppConstants` table constants with zero references anywhere (`usersTable`, `itineraryEventsTable`, `groupsTable`, `groupMembersTable`, `expensesTable`, `messagesTable`) — only `itinerariesTable` was ever actually used. Split `Validators`: `validateEmail`/`validatePassword` moved to a new `lib/features/auth/domain/validators/auth_validators.dart` (`AuthValidators`) — genuinely auth-domain rules; the rest stayed in `core/utils` since they're truly generic. Trimmed `Formatters` from 11 methods to the 3 actually used anywhere (`formatDate`, `formatDateTime`, `formatCurrency`) — the other 8, including the `formatPhoneNumber` this finding named specifically, had zero call sites and zero test coverage.
- Updated `docs/SOLID_AUDIT.md`'s ranked priority list to mark both items ✅ Fixed.
- **Remaining on the list after this round:** #2 (split `SocialRepository`), #3 (Auth's mixed DIP story), #4 (`InviteMemberPage`/`PrivacySettingsPage` bypassing usecases), #6 (`TransportMode`/`KumoTheme` lookup-table refactor), #7 (`edit_profile_page.dart`/`chat_page.dart` direct-Supabase calls), #8 (decouple `core/notifications` from `config/router.dart`), #11 (`ChatRepository.upsertPushToken`, `UserProfileRepository` notification-preference split), #12 (dedupe `_DateTimePickerField`, legal-page widgets, fork/like workflow).

**Verification:** `flutter analyze` clean (no new issues beyond the existing info-level baseline). `flutter test` — all passing, no test behavior changed (type-annotation-only + dead-code removal). `dart format lib/ test/` — clean. Committed as `0b78992`.

## SOLID audit cleanup, round 3 — item #12 dedupe (2026-08-11)

Item #12 (the last-*numbered*, not last-*remaining*, item) from `docs/SOLID_AUDIT.md`'s ranked priority list — 7 items (#2, #3, #4, #6, #7, #8, #11) were still open after this round; see round 4 below for #6.

- [x] **`_DateTimePickerField` (2 copies).** Extracted to `lib/features/itinerary/presentation/widgets/date_time_picker_field.dart` as a public `DateTimePickerField`; `add_edit_item_page.dart` and `add_edit_trip_segment_page.dart` both updated to use it, both private duplicates deleted.
- [x] **Legal-page section widgets (2 copies).** Extracted `_Heading`/`_Section`/`_Body` to `lib/features/legal/presentation/widgets/legal_section_widgets.dart` as public `LegalHeading`/`LegalSection`/`LegalBody`; both `privacy_policy_page.dart` and `terms_page.dart` updated. `privacy_policy_page.dart`'s page-specific `_SubHeading` (not duplicated in `terms_page.dart`) was correctly left untouched.
- [x] **Fork/like workflow (Discover vs. PublicProfile, already drifted).** Extracted to `lib/features/social/presentation/utils/post_actions.dart` (`forkPostWithConfirmation`, `toggleLike`), used by both `discover_page.dart` and `public_profile_page.dart`. This closed the actual behavioral drift the audit flagged, not just the code duplication: `PublicProfilePage` previously forked immediately with no confirmation dialog, unlike `DiscoverPage` — decided to unify on the safer behavior (always confirm before forking) rather than just parameterize the difference away. Each page still owns its own post-action provider refresh, since those genuinely differ by page (`explorePostsProvider`/`followingFeedProvider` vs. `authorPostsProvider`) and were never part of the duplication.
- Updated `docs/SOLID_AUDIT.md`'s SRP table (3 rows marked ✅ Fixed) and ranked priority list (#12 marked ✅ Fixed).

**Verification:** `flutter analyze` — clean (only the existing 265 info-level baseline issues, none new). `flutter test` — **950/950 passing**, including all 14 legal-page tests and all 97 social-feature tests. `dart format lib/` — clean. Not yet committed.

## SOLID audit cleanup, round 4 — item #6 (2026-08-11)

- [x] **`KumoTheme` → visuals fan-out.** `theme.dart`'s per-theme `ThemeData` getters and `main.dart`'s `MaterialApp.theme` switch both independently listed every `KumoTheme` case — the same enum→`ThemeData` mapping duplicated across two files. Collapsed into one `AppTheme.all` lookup map in `lib/config/theme.dart`; `main.dart` now reads `AppTheme.all[kumoTheme]` instead of re-declaring the switch. New `test/config/theme_test.dart` (2 tests) — first coverage `AppTheme` has ever had.
- 🟡 **`TransportMode` → rendering fan-out — deliberately left alone.** Re-examined and found this doesn't match the `KumoTheme` shape: only 2 of the 4 files actually switch on `TransportMode` itself; the other 2 (`route_line_painter.dart`, `route_marker_bitmaps.dart`) switch on the *derived* `RouteTexture` enum, and per `lib/core/maps/CLAUDE.md`'s own documentation their per-map-engine tick/tread-drawing code is **deliberately** kept independently-derived (different coordinate conventions per engine, cross-checked against source SVGs via external scripts specifically so the two don't drift by hand-transcription error) rather than shared. Collapsing it would fight that documented design and is unverifiable here (no device/simulator to visually confirm route rendering on either map engine) — a real correctness risk for a stylistic win. Marked 🟡 partially fixed rather than ✅ in `docs/SOLID_AUDIT.md`, with the reasoning recorded inline so it isn't silently dropped.
- **Remaining after this round:** #2 (split `SocialRepository`), #3 (Auth's mixed DIP story), #4 (`InviteMemberPage`/`PrivacySettingsPage` bypassing usecases), #7 (`edit_profile_page.dart`/`chat_page.dart` direct-Supabase calls), #8 (decouple `core/notifications` from `config/router.dart`), #11 (`ChatRepository.upsertPushToken`, `UserProfileRepository` notification-preference split).

**Verification:** `flutter analyze` — clean (265 issues, unchanged baseline, no new). `flutter test` — **952/952 passing** (+2 from the 950 baseline above). `dart format lib/ test/` — clean. Committed as `c0d3029` (together with rounds 5–6 below).

## SOLID audit cleanup, round 5 — item #11 (2026-08-12)

Two ISP fixes, both genuinely fat/misplaced interface members flagged by the audit.

- [x] **`ChatRepository.upsertPushToken` → `core/notifications`.** Turned out to be dead code at the repository layer: nothing anywhere called `ChatRepository.upsertPushToken`/`ChatRepositoryImpl.upsertPushToken` — `fcmTokenSyncProvider` was already reaching past the repository and calling the raw `ChatRemoteDataSource.upsertPushToken` directly. New `lib/core/notifications/push_token_datasource.dart` (`PushTokenDataSource`/`Impl`, single-method abstract-class+Impl DI seam, matching this codebase's established convention) now owns the `upsert_push_token` RPC call; `fcmTokenSyncProvider` reads the new `pushTokenDataSourceProvider` instead. Removed `upsertPushToken` entirely from `ChatRemoteDataSource`/`Impl` and `ChatRepository`/`Impl` — it had zero test coverage at the repository layer either, confirming it was never really exercised through that path.
- [x] **`UserProfileRepository`'s notification-preference methods split out.** New `NotificationPreferenceRepository`/`Impl` + `NotificationPreferenceRemoteDataSource`/`Impl` (`lib/features/profile/domain/repositories/`, `lib/features/profile/data/`) own `getNotificationPreferences`/`upsertNotificationPreference`; both removed from `UserProfileRepository`/`Impl`/`UserProfileRemoteDataSource`/`Impl`. `notificationPreferencesProvider` and `NotificationPreferencesPage`'s toggle handler now read the new `notificationPreferenceRepositoryProvider`. `EditProfilePage`'s `pushMessagePreviewEnabled` toggle deliberately stayed on `UserProfileRepository.updateProfile` — it's a real column on `profiles`, not a `notification_preferences` row, so it was never part of the fat-interface problem.
- Test coverage moved intact (not just deleted): the 9 `getNotificationPreferences`/`upsertNotificationPreference` tests moved from `user_profile_repository_impl_test.dart` into a new `notification_preference_repository_impl_test.dart`, same assertions, same mock shape, now exercising the new class.
- Updated `docs/SOLID_AUDIT.md`'s ISP section (both findings marked ✅ Fixed) and ranked priority list (#11 marked ✅ Fixed).
- **Remaining after this round:** #2 (split `SocialRepository`), #3 (Auth's mixed DIP story), #4 (`InviteMemberPage`/`PrivacySettingsPage` bypassing usecases), #7 (`edit_profile_page.dart`/`chat_page.dart` direct-Supabase calls), #8 (decouple `core/notifications` from `config/router.dart`).

**Verification:** `flutter analyze` — clean (no new issues beyond the existing baseline; fixed one incidental `comment_references` info in `notification_providers.dart`'s doc comment, a broken doc-link left over from removing the now-unneeded `chat_provider.dart` import). `flutter test` — **952/952 passing**, unchanged count (9 tests moved, not added/removed). `dart format lib/ test/` — clean. Committed as `c0d3029` (together with round 6 below).

## SOLID audit cleanup, round 6 — item #4 (2026-08-12)

`InviteMemberPage`/`PrivacySettingsPage` were the one place in the codebase with no domain layer at all above a datasource — this round built one.

- [x] **New domain layer for profile lookup/invite/searchability.** `lib/features/itinerary/domain/entities/profile_result.dart` (`ProfileResult`, moved out of the datasource file it previously lived in), `lib/features/itinerary/domain/repositories/profile_lookup_repository.dart` (`ProfileLookupRepository`, `Either<Failure, T>` throughout), 5 usecases (`FindProfileByEmailUseCase`, `SearchProfilesByNameUseCase`, `UpdateSearchabilityUseCase`, `GetCurrentUserProfileResultUseCase`, `CreatePendingInvitationUseCase`). Data layer: `ProfileResultModel extends ProfileResult`, `ProfileLookupRepositoryImpl` (standard exception→`Failure` mapping, same shape as every other repository impl this session).
- [x] **One shared provider file replaces two duplicate ones.** `lib/features/itinerary/presentation/providers/profile_lookup_provider.dart` — both `InviteMemberPage` and `PrivacySettingsPage` previously each declared their own page-local `_profileDataSourceProvider` pointing at the same `ProfileRemoteDataSourceImpl`.
- [x] **`InviteMemberPage` rewritten onto usecases.** `_SearchTab`/`_EmailTab` now depend on exactly the one usecase each calls (`SearchProfilesByNameUseCase`/`FindProfileByEmailUseCase`) instead of the full `ProfileRemoteDataSource` interface — closes the matching ISP finding as a side effect. All 3 raw try/catch blocks (search, email lookup, pending-invite creation) replaced with `Either.fold`.
- [x] **`PrivacySettingsPage` rewritten onto usecases.** `_toggle` and `currentUserProfileProvider` replaced their raw try/catch with `Either.fold`, matching `_updateVisibility`'s existing convention in the same file — no more two incompatible error-handling architectures side by side. Deliberately preserved existing error-surfacing behavior: `currentUserProfileProvider` re-throws a real failure (`throw Exception(failure.message)`, this codebase's established convention for turning a `Left` into a `FutureProvider` `AsyncError`) rather than folding it to `null`, since folding to `null` would silently conflate "lookup failed" with "no profile row for this user."
- New tests (first coverage this code path has ever had): `profile_lookup_usecases_test.dart` (10 tests), `profile_lookup_repository_impl_test.dart` (11 tests), `profile_result_model_test.dart` (2 tests). Existing `privacy_settings_page_test.dart` (12 tests) still passes unchanged — confirms the refactor didn't alter observable page behavior.
- Updated `docs/SOLID_AUDIT.md`'s DIP and ISP sections (3 findings marked ✅ Fixed) and ranked priority list (#4 marked ✅ Fixed).
- **Remaining after this round:** #2 (split `SocialRepository`), #3 (Auth's mixed DIP story), #7 (`edit_profile_page.dart`/`chat_page.dart` direct-Supabase calls), #8 (decouple `core/notifications` from `config/router.dart`).

**Verification:** `flutter analyze` clean, `flutter test` 975/975 passing. Committed as `c0d3029` (together with rounds 3–5 above).

## SOLID audit cleanup, round 7 — item #2 (2026-08-12)

- [x] **Split `SocialRepository`'s follow graph into a new `FollowRepository`.** The interface had actually grown to 12 methods by the time this was picked up (comments were added after the finding was originally written — same kind of stale count already corrected for the concrete-repository-provider finding earlier in this session). New `lib/features/social/domain/repositories/follow_repository.dart` (`toggleFollow`/`fetchFollowStats`) + `lib/features/social/data/repositories/follow_repository_impl.dart`. Deliberately **shares** `SocialRemoteDataSource` rather than getting its own datasource — `fetchFeed` also reads the `follows` table to resolve which authors' posts belong in the following feed, so the two concerns are genuinely coupled at the data layer even though they're cleanly separable at the repository/domain layer.
- [x] **Comments stayed on `SocialRepository`, not a third split.** They're conceptually a post concern (a post's comment thread) with no "no consumer needs both" argument as strong as the follow graph had — `SocialRepository` is now 10 methods (publish/fetch/fork/like/delete/comments), down from 12, no longer bundling the one concern that actually had a concrete no-shared-consumer case (`PublicProfilePage`'s follow toggle vs. `DiscoverPage`'s publish flow).
- [x] **Retyped both follow usecases.** `ToggleFollowUseCase`/`FetchFollowStatsUseCase` now depend on `FollowRepository`; new `followRepositoryProvider` added in `social_provider.dart` alongside the existing `socialRepositoryProvider`.
- Test coverage split to match: new `follow_repository_impl_test.dart` (4 tests) and `fetch_follow_stats_usecase_test.dart` (2 tests, moved out of the combined `fetch_social_reads_usecase_test.dart`); `toggle_follow_usecase_test.dart` retyped to mock `FollowRepository` instead of `SocialRepository`.
- Updated `docs/SOLID_AUDIT.md`'s ISP section and cross-cutting-themes note (both marked ✅ Fixed) and ranked priority list (#2 marked ✅ Fixed).
- **Remaining after this round:** #3 (Auth's mixed DIP story), #7 (`edit_profile_page.dart`/`chat_page.dart` direct-Supabase calls), #8 (decouple `core/notifications` from `config/router.dart`).

**Verification:** `flutter analyze` — clean, no new issues. `flutter test` — **977/977 passing** (+2 from the 975 baseline above). `dart format lib/ test/` — clean. Not yet committed.

## SOLID audit cleanup, round 8 — item #3 (2026-08-12)

Scoped exactly to what the finding asked for — the retype plus the one missing usecase, not a full usecase-per-operation pass over `AuthNotifier`.

- [x] **Retyped `authRepositoryProvider`/`AuthNotifier.repository`.** `Provider<AuthRepositoryImpl>` → `Provider<AuthRepository>`, `AuthRepositoryImpl` field → `AuthRepository` field. Pure type-annotation change — `AuthRepositoryImpl` already implemented the full abstract interface, so `getCurrentUser`/`resetPassword`/`updateProfile` (still called directly from `AuthNotifier`, not usecase-wrapped) compiled with zero changes.
- [x] **New `SendPasswordResetEmailUseCase`.** `password_reset_page.dart` now calls it instead of reaching for `ref.read(authRepositoryProvider)` directly — matches every sibling auth flow (`login`/`signup`/`logout`/`deleteAccount`).
- [x] **13 test files retyped their `AuthRepository` mock.** All of them constructed a real `AuthNotifier` (per this codebase's established pattern for testing anything behind `authNotifierProvider`) via `class MockAuthRepository extends Mock implements AuthRepositoryImpl {}` — retyped to `implements AuthRepository` for consistency with the production retype. None of these would actually have broken left as-is (a mock of the concrete class is still a valid `AuthRepository`), but leaving them pointed at the concrete class after retyping the real provider would have been a stale, misleading pattern for the next person copying it.
- New `send_password_reset_email_usecase_test.dart` (3 tests). Existing `PasswordResetPage` coverage in `auth_pages_test.dart` (4 tests) still passes unchanged.
- Updated `docs/SOLID_AUDIT.md`'s DIP section (2 findings marked ✅ Fixed) and ranked priority list (#3 marked ✅ Fixed) — explicitly recorded the scope decision (not touching `AuthNotifier`'s remaining direct repository calls) so it reads as a deliberate choice, not an oversight.
- **Remaining after this round:** #7 (`edit_profile_page.dart`/`chat_page.dart` direct-Supabase calls), #8 (decouple `core/notifications` from `config/router.dart`).

**Verification:** `flutter analyze` — clean, no new issues. `flutter test` — **980/980 passing** (+3 from the 977 baseline above). `dart format lib/ test/` — clean. Not yet committed.

## SOLID audit cleanup, round 9 — item #7 (2026-08-12)

- [x] **`edit_profile_page.dart`'s avatar upload extracted.** New `AvatarRepository`/`AvatarRepositoryImpl` + `AvatarRemoteDataSource`/`Impl` (`lib/features/profile/domain/repositories/`, `lib/features/profile/data/`) own the `avatars` bucket upload + cache-busted public-URL construction. `_pickAndUpload` now calls `ref.read(avatarRepositoryProvider).uploadAvatar(...)` and folds the `Either` result — the page's `package:supabase_flutter` import is gone entirely, it no longer touches Supabase at all.
- [x] **`chat_page.dart`'s attachment upload extracted.** `ChatRepository` gained a 5th method, `uploadAttachment` (returns a `({String storagePath, String publicUrl})` record) — still a cohesive, chat-message-scoped interface, not a re-fattening of the repository I just split in round 7. `_uploadAndSend` now calls `ref.read(chatRepositoryProvider).uploadAttachment(...)`, matching this file's existing convention of reading `chatRepositoryProvider`/`chatRemoteDataSourceProvider` directly for operations that aren't `sendMessage` (the one usecase this feature has). The `uuid` package import is now unused in this file and was removed (attachment path generation moved into the datasource, which already had its own `uuid` import for message IDs).
- 🟡 **Deliberately left two of the four originally-bundled call sites untouched.** `chat_page.dart`'s realtime typing-indicator channel (`KumoSupabaseClient.client.channel(...)`, stateful, widget-lifecycle-bound, zero existing test coverage — abstracting it behind a repository would mean either leaking `RealtimeChannel` into the domain layer or a real redesign of the typing-indicator feature) and `auth_provider.dart`'s `onAuthStateChange` listener (that's `AuthNotifier` itself, the app's own auth abstraction boundary, listening to the SDK to drive its state machine — not "presentation reaching past its repository" in the same sense as the other two). Recorded in `docs/SOLID_AUDIT.md` as 🟡 partially fixed rather than silently dropped.
- New tests: `chat_repository_impl_test.dart` gained an `uploadAttachment` group (2 tests, needed a `registerFallbackValue(Uint8List(0))` for mocktail's `any()` matcher on the new `bytes` param); new `avatar_repository_impl_test.dart` (3 tests).
- Updated `docs/SOLID_AUDIT.md`'s SRP row, DIP section (2 findings, one fully fixed + one partially), and ranked priority list (#7 marked 🟡 partially fixed with the scope decision recorded inline).
- **Remaining after this round:** #8 (decouple `core/notifications` from `config/router.dart`) — the last item on the original list.

**Verification:** `flutter analyze` — clean, no new issues. `flutter test` — **985/985 passing** (+5 from the 980 baseline above). `dart format lib/ test/` — clean. Not yet committed.

## SOLID audit cleanup, round 10 — item #8, ranked list closed out (2026-08-12)

The last item on `docs/SOLID_AUDIT.md`'s ranked priority list.

- [x] **New `notification_tap_events.dart`.** `NotificationTapEvent` (`NotificationTapKind.chat`/`.social` + id) broadcast over a module-level `Stream`. `NotificationService._onTap` and `push_message_handler.dart`'s `handleIosPushTap` both now just parse their payload and call `emitNotificationTap(...)` — neither imports `config/router.dart` or `go_router` anymore.
- [x] **`main.dart` owns the one navigation decision.** Subscribes once (`notificationTaps.listen(_navigateForNotificationTap)`) and maps kind→route in exactly one place — previously duplicated (and would drift) between the two files above.
- [x] **Bonus: `handleIosPushTap` became properly unit-testable.** The existing `push_message_handler_test.dart` could previously only assert `returnsNormally` (no navigator mounted in a test, so the old code's actual `GoRouter.of(context).go(...)` call was structurally unreachable — the assertion never really exercised the routing logic). It now asserts the emitted `NotificationTapEvent`'s `kind`/`id` directly, a real behavioral check.
- Updated `docs/SOLID_AUDIT.md`'s DIP section and ranked priority list (#8 marked ✅ Fixed), plus a closing 2026-08-12 addendum summarizing the full list's status: **10 of 12 items fully fixed, 2 partially fixed with the remaining scope documented inline** (not silently left open). One smaller, not-formally-ranked item (`WorkModeNotifier`/`OnboardingNotifier` structural duplication, noted in the 2026-08-11 addendum) stays open for a future pass.
- **This closes out `docs/SOLID_AUDIT.md`'s ranked priority list** — the last item from the original "complete the remaining 10 cleanup tasks" request.

**Verification:** `flutter analyze` — clean, no new issues. `flutter test` — **985/985 passing**, unchanged count (4 existing tests in `push_message_handler_test.dart` strengthened, not added). `dart format lib/ test/` — clean. Not yet committed.

## WorkModeNotifier/OnboardingNotifier dedup (2026-08-13)

The one smaller, not-formally-ranked SOLID finding left open after round 10 (`docs/SOLID_AUDIT.md`'s 2026-08-11 addendum) — picked up on request.

- [x] **New `lib/core/providers/per_user_bool_preference_notifier.dart`.** `PerUserBoolPreferenceNotifier` — the per-user-scoped `SharedPreferences` `bool?` sync/persist logic (`_lastUserId` caching, `_sync()` on construction and every `authNotifierProvider` change, reset-to-null on sign-out) that `OnboardingNotifier` and `WorkModeNotifier` had independently duplicated. Both now extend it, supplying only their own key prefix and a domain-named write method (`markComplete()` / `setWorkMode({required value})`) delegating to the base's `setValue({required value})`.
- Pure structural refactor — no behavior change, no test file touched (existing coverage for both notifiers still exercises the same public API and passed unchanged).
- Updated `docs/SOLID_AUDIT.md`'s 2026-08-11 and 2026-08-12 addenda to mark this ✅ Fixed.

**Verification:** `flutter analyze --no-fatal-infos` — 11 issues, unchanged baseline (all pre-existing `one_member_abstracts`). `flutter test` — **1081/1081 passing**, unchanged count. `dart format lib/` — clean. Not yet committed.

## 18+ age gate + Hitchhiker non-account collaborator role; private storage buckets; privacy audit fixes (2026-08-12/13)

A legal/data-privacy compliance audit (separate from the three tracked security/scalability/SOLID audits) found the trip-membership model had no age floor and no path for a minor or account-averse guest to participate without becoming a full data subject. Full design rationale in `docs/ARCHITECTURE.md`'s "Trip Roles" section and `lib/features/hitchhiker/CLAUDE.md`.

- [x] **New migration `docs/supabase_migrations/stage44_age_gate.sql`.** `enforce_signup_age_gate()` — a `BEFORE INSERT` trigger on `auth.users` that computes age from a DOB supplied at signup and **rejects the row outright** if under 18 (no account of any kind is created). Strips raw DOB out of `raw_user_meta_data` before persisting anything, keeping only `profiles.age_verified_at` (pass/fail, not the DOB itself). A separate `confirm_age` RPC + `ConfirmAgePage`/`AgeGateNotifier` flow covers invite-created accounts, which never go through signup's own DOB field.
- [x] **New migration `docs/supabase_migrations/stage45_hitchhikers.sql`.** New `trip_hitchhikers` table — a Hitchhiker has **no `auth.users` row, no `user_id`, ever**; added by a Captain with just a first name. Access is a bearer token (`access_token`), validated by `SECURITY DEFINER` RPCs (`create_hitchhiker`, `revoke_hitchhiker`, `hitchhiker_get_trip_view`, `hitchhiker_send_message`, `hitchhiker_suggest_item`) rather than RLS, since RLS is built on `auth.uid()` and a Hitchhiker never has one. Anonymous Supabase Auth was considered and rejected for this — it would still create an `auth.users` row, weakening "zero identity-system rows for a minor" down to "anonymized rows." Revocation is immediate.
- [x] **Why this is regulatory, not cosmetic:** the two-tier split (full-account Captain/Crew, always 18+, vs. account-less Hitchhiker) keeps Kumo outside COPPA (no data collected from/about anyone under 13 — no independent record exists outside the one trip), the UK/EU Age Appropriate Design Code (applies to any under-18 *account* holder, and Hitchhikers never have one), and GDPR's 13–16 digital-consent thresholds (moot with no independent data subject). Structural, not a checklist: Hitchhikers can't reach `itinerary_posts`, marketing email, push preferences, or `xp_events` — every one of those tables FKs to `auth.users`/`profiles`, which a Hitchhiker never has.
- [x] **New `lib/features/hitchhiker/` module** — full Clean Architecture layers (`Hitchhiker`/`HitchhikerTripView` entities, `HitchhikerRepository`/`HitchhikerAccessRepository` + usecases), owner-side `HitchhikerTab` (roster, add via name-only form + QR/share-link, revoke with confirmation), and an isolated `/hitchhiker/:token` `HitchhikerScreen` — no login, no app shell, just the token-scoped trip view, chat send, and packing-suggestion submit. `lib/features/itinerary/domain/entities/trip_role.dart` exposes `TripRole { captain, crew, hitchhiker }` + `resolveTripRole(...)` as the shared classifier.
- [x] **Private Storage buckets (same pass, `stage43_private_avatars_chat_attachments.sql`).** `avatars`/`chat-attachments` flipped from `public=true` to `public=false` — previously readable by anyone with the object URL, no auth or app-level control, despite `chat-attachments` being able to hold photographed travel documents. Avatars now reuse `profiles_select`'s visibility logic (owner, public profile, or searchable); chat-attachments require trip membership. New `lib/core/network/signed_storage_url.dart` (`signedStorageUrlProvider`) + `lib/shared/widgets/kumo_avatar.dart` (`KumoAvatar`) resolve signed URLs at render time, replacing raw `NetworkImage(getPublicUrl())` at every avatar/attachment call site. **Deployment-coupled:** this migration and the depending app build must ship together — an old client hitting the old public URL gets a 403 once the buckets flip.
- [x] **Privacy-audit quick wins, same pass:** wired the existing GDPR data-export RPC to a "Download my data" action in Privacy Settings (`ExportOwnDataUseCase`); stopped `invite-email`/`inbound-trip-email` from logging raw Resend error-response bodies (could echo recipient email addresses into Supabase function logs — now logs only the HTTP status); corrected a privacy-policy claim about a CSV expense-export feature that never existed; `stage42_trip_email_log_retention.sql` adds a 90-day purge for `trip_email_forward_log.from_address` (a third party's email), scheduled via `pg_cron` where enabled.
- [x] **Real pre-existing bug found while reissuing `handle_new_user()` for the age gate:** `stage21_trip_segments.sql` had silently dropped the pending-invitations auto-join loop `stage2b` originally added — fixed as part of the same reissue.
- [x] **Test coverage follow-up (`b6310bc`), same day.** The feature commit shipped with partial coverage; closed the gaps immediately after: `ConfirmAgeUseCase`, `AuthRepositoryImpl.confirmAge()`, `AgeGateNotifier` (signed-out/verified/unverified/failed-fetch/re-sync states), `ConfirmAgePage` widget test; `RevokeHitchhikerUseCase`, `ListHitchhikersUseCase`, `GetHitchhikerTripViewUseCase`, `HitchhikerAccessRepositoryImpl`, `hitchhikerJoinLink()`; `HitchhikerTab` widget test (roster states, add-flow calls `createHitchhiker` and opens the share sheet, remove-flow confirms then calls `revokeHitchhiker`) and `HitchhikerScreen` widget test (loading/error states, chat send, suggestion submit).
- **Deliberate, documented, not built:** Hitchhiker → Crew promotion (a Hitchhiker who turns 18 or wants an account goes through normal signup separately; no automatic row-conversion migration path — more scope than this pass warranted, revisit if it becomes a real product ask). No rate limiting yet on the token-authenticated Hitchhiker RPCs — deliberately deferred, same posture as SEC-021's like/follow deferral (`access_token` is a random UUID shared only with the invited person, instantly revocable).

**Verification (combined, as of `1768d83`):** `flutter analyze --no-fatal-infos` clean at the tracked baseline. `flutter test` — **1081/1081 passing**. Committed as `cd2c40b` (feature) + `b6310bc` (coverage follow-up). **Not yet run against the live database** — see the deployment-gaps list above.

## Flutter 3.44.8 → 3.47.0 upgrade (2026-08-13)

Routine SDK upgrade, not audit- or feature-driven.

- [x] Fixed two real `unawaited_return_in_try_block` issues the upgraded analyzer now catches — returning a `Future` directly inside a `try` block means the enclosing `catch` never actually handles errors from it, since Dart doesn't await it before the block exits. Added the missing `await` in `SendMessageUseCase.call()` and `UserProfileRemoteDataSourceImpl.updateProfile()`.
- [x] `pubspec.lock` — transitive SDK-bundled packages (`matcher`, `meta`, `test_api`, `vector_math`) bumped to match the new Dart SDK; no direct dependency changes. `analysis_options.yaml` picked up the newer tooling's default platform-directory excludes. `ios/Podfile`/`Podfile.lock` — template/checksum sync from an iOS build under the new Flutter version (the one line that changed is a disabled comment; the real deployment target was already 15.0, unchanged).

**Verification:** `flutter analyze --no-fatal-infos`, `dart format`, full test suite (**1081/1081**), an Android debug build, and an iOS simulator build all pass post-upgrade. Committed as `665ca6a`.

## Analyzer info-level cleanup: lib/ then test/ (2026-08-13)

Closed out the accumulated info-level analyzer backlog (11 `one_member_abstracts` aside, which are intentional per `lib/core/maps/CLAUDE.md`'s documented DI convention).

- [x] **`lib/` — 100 issues down to 11.** `dart fix --apply lib/` (28 automated fixes) plus manual fixes: migrated all 4 `Share.share()` call sites to `SharePlus.instance.share(ShareParams(...))` (`deprecated_member_use`); control-body/cascade-invocation nits, mostly canvas-drawing code (`route_line_painter.dart`, `route_map_view.dart`, `route_marker_bitmaps.dart`); doc-comment `[symbol]` references switched to backticks; `avoid_positional_boolean_parameters` — converted `setVisibility(id, bool)` → `setVisibility(id, {required bool isVisible})` through the full stack (datasource → repository → usecase → UI → both test files). The remaining 11 are all `one_member_abstracts`, deliberately left — the abstract-class+Impl+Provider pattern for single-method services is this codebase's established convention, not an oversight.
- [x] **`test/` — 0 remaining.** `dart fix --apply test/` (253 automated fixes) plus 4 manual fixes: three repeated-receiver call sequences converted to cascades, and `_StubWeatherService`'s error field narrowed from `Object?` to `Exception?` to satisfy `only_throw_errors`.
- No behavior changes in either pass — mechanical lint fixes only.

**Verification:** `dart format`, `flutter analyze --no-fatal-infos` (clean, exit 0), full test suite (**1081/1081** both times, unchanged count) after each pass. Committed as `1768d83` (lib/) and `0f0376e` (test/).

## Doc catch-up pass: bring tracked docs current through the age-gate/Hitchhiker work (2026-08-13)

Requested ("update all docs") after this file itself was found 5 commits stale. Two parts: a file move that had happened outside this session, and a real content review of the three actively-tracked audit docs.

- [x] **`Checklist.md` moved from the repo root to `docs/Checklist.md`.** Found mid-session as an uncommitted working-tree change neither this session nor its immediate predecessor had made — confirmed with the user before proceeding (byte-identical content, pure move). Every cross-reference to `Checklist.md` across the repo updated to `docs/Checklist.md`: `docs/DEVELOPMENT_ROADMAP.md` (6 refs, plus dropping the now-inaccurate "(repo root)" qualifier), `docs/SECURITY_AUDIT.md` (1 ref), `lib/features/notifications/CLAUDE.md`, `lib/config/router.dart`, and 3 migration file comments (`stage37`/`stage38`/`stage39`).
- [x] **`docs/SECURITY_AUDIT.md` — reviewed Stage 24 (age gate + Hitchhiker) against the same "verified, not assumed" bar as every other pass in this doc, not just summarized from the feature's own commit message.** Two new findings:
  - **SEC-033 (Medium, open, flagged not fixed):** grepped `age_verified_at` across all 45 migrations — referenced only in `stage44_age_gate.sql` itself. No RLS policy or RPC anywhere else checks it, so the invite-created-account age gate (`AgeGateNotifier`'s own doc comment calls it "a real authorization gate") is currently enforced entirely by the Flutter router redirect, not the database. A scripted client with a valid invite-path session could skip `/confirm-age` and use every other RLS-permitted table/RPC while remaining permanently unverified — undermining the exact regulatory goal (COPPA/UK-AADC/GDPR) this feature exists for, for that one account-creation path. Remediation sketched (a DB-level `age_verified_at` check on the write paths that matter) but not applied — real scope/blast-radius decision, not a one-line patch, left for the user.
  - **SEC-034 (Low, accepted risk, not a new decision — just newly tracked):** Hitchhiker's token RPCs have no rate limit, already documented at ship time in the migration itself with the same reasoning as SEC-021's like/follow deferral. Logged as its own numbered finding so it's in this doc's registry, not only in a migration comment.
  - Executive summary, findings table, and open-issues count updated (32 of 34 now resolved; SEC-014 and SEC-033 open).
- [x] **`docs/SCALABILITY_AUDIT.md` — fourth pass.** Logged the gamification scalability review that had happened (per this file's own "Audit round: gamification review" entry above) but was never written into that doc. Reviewed Stage 24 fresh: no scalability finding — `enforce_signup_age_gate()` is constant-time single-row trigger logic, `hitchhiker_get_trip_view()`'s reads are bounded/trip-scoped, `trip_hitchhikers` has no self-serve growth vector. The one real finding from this pair (no RPC rate limit) is an abuse concern, tracked as SEC-034 above rather than duplicated here.
- [x] **`docs/DEVELOPMENT_ROADMAP.md`** — new **Stage 24** entry (age gate + Hitchhiker, private storage buckets, privacy-audit quick wins), timeline diagram, "What Remains" table (added SEC-033), Quality Gates (analyzer/test counts refreshed to the current 11/1081, migration-liveness caveat added), and Outstanding list (stage42–45 deployment gap + the stage43/app-build coupling note) all brought current. Version bumped 2.1 → 2.2.
- [x] **Feature `CLAUDE.md`s cross-referenced the new findings** — `lib/features/auth/CLAUDE.md` now caveats `AgeGateNotifier`'s "real authorization gate" claim with SEC-033; `lib/features/hitchhiker/CLAUDE.md`'s existing rate-limit-deferral note now cites SEC-034 by number.
- **Deliberately not touched:** `docs/ARCHITECTURE.md` — already current for Stage 24 (its "Trip Roles" section was written the same day as the feature, 2026-08-12, and explicitly flagged as "real and shipped, unlike most of the rest of this document"); no changes needed. `docs/DEVELOPMENT_LOG.md`/`TESTING_STRATEGY.md`/`UNIT_TESTS.md`/`DARTDOC_GUIDELINES.md` are static reference docs dated June 2026 with no per-feature update convention — left alone rather than force a change onto docs that don't track day-to-day state.

Docs-only pass — no Dart/SQL logic changed, so no analyzer/test delta to report. Not yet committed.

## Real device/simulator smoke test, part 1: two crashes found and fixed on `itinerary_detail_page.dart` (2026-08-13)

The QA-gap follow-up from the "how to fix QA gaps" conversation. This dev environment has no attached display (`screencapture` fails outright) and no iOS/Android simulator, so the usual real-device smoke test wasn't possible directly — worked around it by driving the Flutter **web** build (`flutter run -d web-server --dart-define-from-file=env.local.json`) with a small local Playwright + headless-Chrome harness (scratch scripts, not committed), screenshotting each screen. Logged in against the live Supabase project with a real test account (user-supplied credentials) rather than a synthetic one.

- [x] **Login, Home, Profile, Achievements all confirmed rendering correctly** against live data — `GamificationCard` (Level 1, 0/8 badges, XP bar), the private-bucket signed-URL avatar, and the full 8-badge Achievements grid all matched expectations. The no-auth `/hitchhiker/:token` route was also confirmed end-to-end: an invalid token correctly hits the real `hitchhiker_get_trip_view` RPC and renders the intended "This link is no longer valid" error state.
- [x] **Bug found — Start/End/Duration overview pills overflow.** `itinerary_detail_page.dart`'s "Overview pill row" (the three `_InfoPill`s) sized each pill to its own natural width with `mainAxisAlignment: MainAxisAlignment.spaceAround` and no flex — at real phone-width viewports (430px), three date/duration pills together overflowed a `RenderFlex` by 31px. Reproduced cleanly under plain `flutter test` once pointed at a 430×932 surface (this page had zero prior test coverage, so nothing had ever pumped it at a realistic width). Fixed by wrapping each `_InfoPill` in `Expanded` and adding `maxLines: 1` / `overflow: TextOverflow.ellipsis` / centered text to its value label, so long localized dates truncate instead of overflowing.
- [x] **Second, separate bug found — Publish button crashes only under the web (CanvasKit) renderer.** Fixing the pill overflow did *not* fix this — confirmed by rebuilding and re-testing against the live web app, where the *same* `FilledButton` ("Publish"/"Publish update" in `_StatusRow`) kept throwing `BoxConstraints forces an infinite width` and cascading into ~10 downstream assertion failures, leaving the entire Itinerary tab blank for any trip owner viewing their own trip. **This one never reproduced under plain `flutter test`'s default renderer at all** — confirmed by a `FlutterError.onError`-capturing repro test that caught zero errors post-pill-fix, and `flutter test --platform chrome` wasn't usable here (hangs at `setUpAll`, since `initTestSupabase`'s VM-only test mocks for `flutter_secure_storage`/Supabase don't work under that platform). Root-caused and fixed empirically against the real web build instead: wrapping the `FilledButton`/`TextButton` in `Flexible` (changing which `RenderFlex` code path sizes it) resolved it cleanly, verified by rebuild-and-reshoot. The exact CanvasKit-specific mechanism inside Flutter's `ButtonStyleButton`/`RenderFlex` interaction wasn't fully traced beyond that — flagging as unresolved-in-depth rather than claiming full understanding.
- [x] **New `test/features/itinerary/presentation/pages/itinerary_detail_page_test.dart`** — this page had zero coverage before today. 2 tests (draft/unpublished and published owner views) lock in the pill-overflow regression at a real phone-width viewport; the CanvasKit-only bug is *not* re-provable by this harness (documented inline in the test file's header comment), so a web smoke-test remains the real regression guard for that half of the fix.
- **Real, unrelated observation surfaced along the way, not chased:** the test account's own "KumoTest" trip shows a different user ("dudu") tagged **Owner** in the Travellers list while the logged-in account ("arpit") is tagged **Editor** — yet the Publish/Status controls rendered, which only happens when `_isOwner` (`itinerary.ownerId == currentUserId`) is true for the logged-in user. Not investigated further this pass; worth a look if member-role display and actual `ownerId` are meant to always agree.

**Verification:** `flutter analyze --no-fatal-infos` — 11 issues, unchanged baseline. `flutter test` — **1083/1083 passing** (+2 from the 1081 baseline above). `dart format` — clean. Committed as `bb84325`.

## Real device/simulator smoke test, part 2: Hitchhiker end-to-end, and a third crash found (2026-08-13)

Continuation of part 1, focused on Private Mode (Work Mode needs org entities to exist first, deferred). All 6 itinerary detail tabs (Itinerary, Route, Notes, Expenses, Reviews, Packing) confirmed rendering correctly with real data — including the Route tab's live OpenStreetMap rendering with real route markers/lines, and Expenses' budget overview + settle-up. Then drove the full Hitchhiker flow end-to-end for the first time:

- [x] **"Add Hitchhiker" tab, creation, and the real (not fake-token) guest experience all confirmed working against live data.** Created a real Hitchhiker via `invite_member_page.dart`'s third tab — roster, QR code, "Copy link"/"Share" sheet all rendered correctly. Captured the real `access_token` from the `create_hitchhiker` RPC response (network interception, not guessed) and visited `/#/hitchhiker/<real-token>` signed out: the Chat tab showed real trip message history with no login, the Suggest tab rendered its form, and sending a chat message as the Hitchhiker succeeded with no errors.
- [x] **Bug found — a Hitchhiker's chat message permanently crashes the Captain/Crew's own group chat.** Opening the trip's Group Chat as the logged-in owner after the Hitchhiker's message landed threw `TypeError: null: type 'Null' is not a subtype of type 'String'` and rendered nothing at all — not just that message, the *entire* chat history. **Root cause:** `MessageModel.fromJson` (`lib/features/chat/data/models/message_model.dart`) did `senderId: json['sender_id'] as String` — a hard, non-nullable cast. `stage45_hitchhikers.sql` made `messages.sender_id` nullable specifically so a Hitchhiker-authored row can set `hitchhiker_id` instead (`messages_sender_xor_hitchhiker` check constraint) — `lib/features/chat/CLAUDE.md` (last touched at Stage 19, before Hitchhiker existed) was never updated for this, and it's the one integration point the Hitchhiker feature's own design doc didn't cover.
- [x] **Fixed** by falling back to the row's own `hitchhiker_id` when `sender_id` is null (`senderId: (json['sender_id'] as String?) ?? (json['hitchhiker_id'] as String?) ?? ''`) rather than an empty string — keeps `chat_page.dart`'s "same sender" consecutive-message grouping correct even when two different Hitchhikers post in the same trip, and can never collide with a real user's id (disjoint uuid spaces), so `isMe` comparisons stay correct too. 3 new tests in `message_model_test.dart` (fallback-to-hitchhiker_id, two-Hitchhikers-stay-distinct, both-absent-still-empty-not-crash) — this file had coverage for everything else already, just not this case.
- [x] **Verified fixed end-to-end, not just unit-tested:** rebuilt the web app, reopened the group chat as the Captain — full history renders, and the Hitchhiker's message shows up correctly attributed by name in the thread.
- **Not yet checked:** whether a Hitchhiker's message shows correctly in push-notification previews — `hitchhiker_send_message` (the RPC the Hitchhiker screen actually calls) is a plain insert with no push invocation at all, unlike `ChatRemoteDataSourceImpl.sendMessage`'s client-triggered `send-message-push` call. Not confirmed whether that's an intentional scope trim (matching the badge-unlock no-push precedent) or a gap — flagging, not deciding.
- **Checked, found safe:** the "who's read this" long-press receipt sheet (`_MessageBubble._showInfo`) is gated `if (!isMe) return;` — since a Hitchhiker's fallback `senderId` can never equal a real signed-in `currentUserId`, `isMe` is always `false` for a Hitchhiker's message, so this sheet is structurally unreachable for one. Not a gap.

**Verification:** `flutter analyze --no-fatal-infos` — 11 issues, unchanged baseline. `flutter test` — **1086/1086 passing** (+3 from the 1083 baseline above). `dart format` — clean. Committed as `e20d29a`.

## Hardening the three bug-regression tests (2026-08-13)

Requested directly ("write more test to uncover the bugs you found") before continuing further smoke-testing — strengthening the regression coverage added in parts 1–2, not hunting for unrelated new bugs.

- [x] **`itinerary_detail_page_test.dart` — added the untested `_isOwner == false` branch.** All 3 prior tests used the trip's actual owner; a non-owner (Crew/viewer) had never been pumped through this page at all. New test confirms a non-owner renders without exception and never sees the owner-only `FilledButton`/"Not published" publish controls (the Status row itself is unconditional — only the publish-status row below the `Divider` is `_isOwner`-gated, corrected from an initially-wrong assertion caught by the test itself failing on first run).
- [x] **`itinerary_detail_page_test.dart` — new structural guard for the `Flexible` wrapper.** The web/CanvasKit crash (part 1) isn't reproducible in this test harness at all, so nothing stops a future refactor from "simplifying away" the `Flexible` wrap around the Publish button — it looks like a no-op under this suite's renderer. New test asserts the `Flexible` ancestor is still present; paired with a code comment on the wrap itself in `itinerary_detail_page.dart` explaining why it's there and pointing at this test.
- [x] **Checked the Hitchhiker-message fix's blast radius rather than assuming the model-level fix was sufficient.** Traced every `senderId`/`senderName` use in `chat_page.dart`: `isMe` comparison and "same sender" grouping both already correct with the `hitchhiker_id` fallback (covered by the 3 tests from part 2); the read-receipt sheet is `isMe`-gated and therefore structurally unreachable for a Hitchhiker message (documented above, not a gap); no avatar-by-`senderId` lookup exists in `_MessageBubble`. One real open question surfaced and documented above, not fixed: Hitchhiker messages never trigger `send-message-push` at all, since they go through a separate RPC that doesn't invoke it.
- **Deliberately not attempted:** a full `chat_page_test.dart` (this page has zero test coverage, same as `itinerary_detail_page.dart` before part 1) — `docs/SOLID_AUDIT.md` already documents why the typing-indicator's raw Realtime channel makes this page hard to test in isolation; a proper first test suite for it is a separate, larger undertaking than hardening today's three specific regressions.

**Verification:** `flutter analyze --no-fatal-infos` — 11 issues, unchanged baseline. `flutter test` — **1088/1088 passing** (+2 from the 1086 baseline above). `dart format` — clean. Committed as `c31f210`.

## Real device/simulator smoke test, part 3: Work Mode, and a fourth real bug (2026-08-13)

Work Mode had never been reachable for testing before this — the app has **no self-serve path to create an organization from a zero-org account**: the only in-app entry point to `/organizations` (including the "Create Organization" button) is Profile's "My Organizations" tile, and that tile is itself gated behind `isWorkModeAvailableProvider` (already having ≥1 org). A fresh account has no discoverable way to create its first org at all — flagging as a real gap, not fixed this pass (out of scope — the user explicitly deferred the admin-portal question and asked for a quick manual unblock instead). Unblocked with a one-time manual SQL seed (`organizations` + `org_members` rows via the Supabase SQL editor, `role='owner'` on the membership row since that column, not `organizations.owner_id` alone, drives `is_org_admin()`/RLS).

With a real org in place:

- [x] **Work Mode toggle, retheme, and banner all confirmed working** against live data — "Kumo — for Acme Corp" banner, full Onyx & Gold retheme, "Theme: Onyx & Gold (locked by Work Mode)" on the Appearance tile, all rendering correctly.
- [x] **`visibleItinerariesProvider` trip filtering confirmed correct** — the personal "KumoTest" trip (no `org_id`) correctly disappears from Home while Work Mode is active, replaced by the "No trips yet" empty state.
- [x] **Bug found — `CreateItineraryPage` crashes in Work Mode for every organization.** Tapping "Plan a Trip" while in Work Mode threw `Exception: Could not embed because more than one relationship was found for 'org_cost_fields' and 'org_cost_field_sources'` inside `CostFieldPicker`, leaving a red error box in place of the cost-field section (rest of the form — name, dates, budget, theme, the "This trip will be tagged to Acme Corp." notice — rendered fine around it). **Root cause:** `org_cost_field_sources` (`stage30_org_cost_fields.sql`) has *two* foreign keys to `org_cost_fields` — `generated_field_id` and `source_field_id` — and `OrganizationRemoteDataSourceImpl._costFieldsEmbedSelect`'s embed (`org_cost_field_sources(source_field_id, position)`) never told PostgREST which one to use. This is a query-*planning*-time error, not a data-dependent one — it would fail identically for every org regardless of how many cost fields or sources exist, meaning **Work Mode's trip-creation flow has been broken for every organization since Stage 21 shipped (2026-08-10)**, never caught because nothing had ever actually exercised it against a real Postgres schema until today.
- [x] **Fixed** by adding the PostgREST relationship hint: `org_cost_field_sources!generated_field_id(source_field_id, position)`. Verified end-to-end against the live web build — `CostFieldPicker` now renders cleanly (empty, since Acme Corp has zero cost fields configured) and "Create Trip"/"Generate with Katha" show correctly below it.
- **Not test-coverable by `flutter test`:** this bug is a live-Postgres query-shape error; every existing test for `fetchOrgCostFields` mocks the datasource method itself, never reaching the real `.select()` string, so nothing here was skippable-but-missed — this class of bug is structurally invisible to this repo's test suite. Live verification (as done here) is the only guard available; noting the pattern in case it recurs elsewhere `org_cost_field_sources`-shaped self-referencing tables get embedded.

**Verification:** `flutter analyze --no-fatal-infos` — 11 issues, unchanged baseline. `flutter test` — **1088/1088 passing**, unchanged count (no test added — see above). `dart format` — clean. Committed as `273f001`.

## Incident: a real trip was deleted by automated testing, and the resulting fix (2026-08-13)

While testing Personal↔Work Mode switching, a race in the test automation (blind toggle-then-navigate instead of checking persisted state first) left the wrong screen showing at the moment a delete tap fired, and the trash icon on the Home page's trip card was tapped on the user's real **"KumoTest"** trip instead of the disposable "WorkTripTest" test fixture. **The user confirmed it's fine to leave deleted and to continue** — no recovery attempted.

Root cause investigated because it revealed a real, separate product gap:

- [x] **Bug found — the Home/Trips list delete button had no confirmation at all**, unlike the detail page's own delete button (`itinerary_detail_page.dart`'s `_confirmDelete`, a proper `AlertDialog`). `ItineraryCard`'s trash `IconButton` called `onPressed: onDelete` directly — one tap, no dialog, hard `DELETE FROM itineraries` with cascading foreign keys (route segments, expenses, chat messages, notes, packing items — everything scoped to that trip). Used identically by both `home_page.dart` and `trips_page.dart`.
- [x] **Fixed** by adding the same confirmation `AlertDialog` (matching the detail page's copy/style) directly inside `ItineraryCard`, so both call sites get it for free — `onPressed` now calls a new `_confirmDelete(context)` that only invokes the `onDelete` callback if the user taps "Delete". New `test/features/itinerary/presentation/widgets/itinerary_card_test.dart` (first coverage this widget has ever had) — 4 tests: no icon when `onDelete` is null, tapping shows the dialog without deleting, confirming deletes, cancelling doesn't.
- [x] **Verified live** against the web build: dialog renders correctly with the real trip's name interpolated, Cancel dismisses without firing a DELETE request (confirmed via network-request logging, not just visually), leaving the trip intact.

**Also hardened the test automation itself, not just the app**, since the automation's own flaw was the proximate cause: every subsequent script in this session's remaining scripts checks `localStorage`'s actual persisted `work_mode_<uid>` value before deciding whether to tap the toggle, rather than assuming a click always lands — see the reusable harness added below.

**Verification:** `flutter analyze --no-fatal-infos` — 11 issues, unchanged baseline. `flutter test` — **1092/1092 passing** (+4 from the 1088 baseline above). `dart format` — clean. Committed as `b4f13d1`.

## Reusable web smoke-test harness (2026-08-13)

Requested directly ("automate as much as possible for future") — this whole session's smoke-testing had been ad hoc scratch scripts, rewritten and re-debugged from zero each time. New `scripts/web_smoke_test/` captures it as a documented, reusable tool instead.

- [x] **`driver.js`** — the reusable Playwright library: `launch`/`tap`/`typeText`/`login`/`workModeState`/`ensureWorkMode`/`goHome`/`screenshot`. Every function's doc comment explains *why* it's shaped the way it is, not just what it does — each one exists because the "obviously correct" version was flaky in practice this session: `tap()`'s explicit press duration (a zero-delay `.click()` registers as a hover under CanvasKit, not a tap — the exact mechanism behind several apparently-random failures earlier in this session), `ensureWorkMode()`'s check-before-toggle (the direct cause of the KumoTest deletion incident above — a blind toggle assumed a state instead of reading it).
- [x] **`README.md`** — setup, usage, and a dedicated safety section written directly off today's incident: never call a destructive action against a coordinate that isn't freshly re-verified this run, prefer disposable `SMOKETEST-`-prefixed fixtures over the account's real data, check persisted state before acting on it, treat irreversible actions (e.g. publishing) as always needing a fresh human go-ahead. Also documents real limitations: no accessible DOM (CanvasKit renders to one `<canvas>`, so every interaction is a raw pixel coordinate), custom URL schemes untestable on web, and that a bug not reproducing here doesn't mean it's web-only — two of this session's four bugs had web-specific *symptoms* but platform-agnostic *causes*.
- [x] **`example_login_and_home.js`** — copy-and-start template for new scripts.
- [x] **Verified the harness itself works**, not just written and assumed correct: ran it against the live web build, confirmed session reuse via `auth.json` and a real screenshot came back correctly.
- `.gitignore` — `scripts/web_smoke_test/{node_modules,auth.json,screenshots}/` excluded (regenerable, a real session token, and throwaway output, respectively); `package.json`/`package-lock.json` committed for reproducibility.
- Documented in `scripts/CLAUDE.md` alongside the existing icon-generation and git-hooks entries, matching this directory's established documentation convention.

Not committed yet.

## Doc + test coverage re-pass, and a sixth real bug (2026-08-13)

Requested directly ("do another parse over documentation and test case. Fix/add anything needed").

**Docs:**
- [x] **`lib/features/chat/CLAUDE.md`** — added a "Hitchhiker-authored messages" section documenting the `sender_id`-nullable integration point and the crash fix from part 2 above; this doc predated Hitchhiker by several stages and had zero mention of it.
- [x] **`docs/DEVELOPMENT_ROADMAP.md`** — Stage 24 entry gained a "Web smoke-test pass" note summarizing all 5 bugs found+fixed that day; the "Widget + integration tests" row and Outstanding list corrected a since-stale claim that Work Mode/gamification/Hitchhiker had "never been smoke-tested" — they now have (via web), just not on a native device/simulator, which remains genuinely outstanding; test counts refreshed 1081 → 1092.

**Test coverage audit:** grepped every `presentation/pages/*.dart` against `test/` for a matching file, then checked each apparent gap against combined test files (e.g. `auth_pages_test.dart`, `legal_pages_test.dart`) before treating it as real — same false-positive-filtering methodology as every earlier coverage pass this project. **15 pages confirmed genuinely zero-covered**: `add_expense_page`, `add_edit_item_page`, `invite_member_page`, 6 organization settings pages, `home_page`, `splash_page`, `chat_page`, `edit_profile_page`, `notification_preferences_page`, `public_profile_page`. Full list not closed out this pass — prioritized the two highest-value, most-tractable ones given today's other changes touched them directly:

- [x] **New `test/features/home/presentation/pages/home_page_test.dart`** (first coverage `home_page.dart` has ever had) — 6 tests: loaded/empty/error states, search filtering (match and no-match), and the delete-confirmation flow wired end-to-end (tap → dialog → confirm → `DeleteItineraryUseCase` called → card removed) — the real integration this session's `ItineraryCard` fix was meant to protect, not just the isolated-widget version.
- [x] **Bug found while writing `test/features/shell/inbox_page_test.dart`** (also first coverage this page has ever had): **`InboxPage` read `itineraryListProvider`'s raw, unfiltered list directly** instead of `visibleItinerariesProvider` like `home_page.dart`/`trips_page.dart` correctly do — so a Private Mode account saw org-tagged trip chats mixed in with personal ones (and vice versa in Work Mode). This is the exact bug reported live mid-session ("personal inbox show worktrip info") — found in the wild by the user before the test that would have caught it was even finished. Fixed by watching `visibleItinerariesProvider` alongside the existing `itineraryListProvider` state switch, mirroring `trips_page.dart`'s established pattern exactly.
- [x] **Second, related bug found while writing the regression test, not by the live report:** unlike `home_page.dart`, `InboxPage` had no `authNotifierProvider` state-change listener — only a one-time `postFrameCallback` check. If auth was still resolving (`AuthInitial`/`AuthLoading`) at that single first frame — a real race on cold start, not just a test artifact, per the same class of race already documented elsewhere in this app (`CreateItineraryPage`, `PostFeedNotifier`) — the itinerary fetch never fires and the page is stuck on its loading spinner forever, unless some other page (typically Home) happened to already populate the shared `itineraryListProvider` first. Fixed by adding the same `ref.listenManual<AuthState>` pattern `home_page.dart` already uses, factored into a shared `_load()` method.
- [x] **Both fixes verified live**, not just unit-tested — used the new `scripts/web_smoke_test/` harness (dogfooding it) to confirm Inbox correctly filters in both directions post-fix: empty in Private Mode (KumoTest is gone, WorkTripTest is org-tagged), showing only WorkTripTest once switched to Work Mode.
- **Remaining 13 zero-coverage pages, not attempted this pass:** `add_expense_page`, `add_edit_item_page`, `invite_member_page`, the 6 organization settings pages, `splash_page`, `edit_profile_page`, `notification_preferences_page`, `public_profile_page`. `chat_page` is deliberately excluded from that list — already documented in `docs/SOLID_AUDIT.md` and this session's part 2 entry as hard to test in isolation due to its raw Realtime typing-indicator channel, a separate, larger undertaking than this pass.

**Verification:** `flutter analyze --no-fatal-infos` — 11 issues, unchanged baseline. `flutter test` — **1101/1101 passing** (+9 from the 1092 baseline above). `dart format` — clean. Not yet committed.

## Closing the remaining 13 zero-coverage pages (2026-08-13)

Picked up the list left open above. Split across 4 parallel passes covering disjoint files, each writing widget tests (mocktail-mocked usecases/repositories via Riverpod `ProviderScope` overrides, no live network/Supabase — the safe form of regression testing, as opposed to the live web-smoke-test form used in the entries above) and fixing any real bug found along the way, per this project's established pattern.

- [x] **`add_expense_page`, `add_edit_item_page`, `invite_member_page`** — 16 new tests. **Two real bugs found and fixed**, same root cause: both pages only ever `.read()` the itinerary provider inside submit handlers instead of `.watch()`ing it in `build()`, so a cold entry (e.g. a fast deep link, before the parent detail page's stream had resolved) could hit a null itinerary. `add_edit_item_page.dart`'s add-mode submit failed with "Trip data not available, please try again" every time under that race; `invite_member_page.dart`'s `_addMember` silently no-opped with **no user feedback at all**. Both fixed by watching the provider unconditionally at the top of `build()` and rendering a loading state until it resolves — matching `add_expense_page.dart`'s existing (correct) pattern. Same race class already on record for `CreateItineraryPage`/`InboxPage`/`PostFeedNotifier`.
- [x] **`create_organization_page`, `organizations_list_page`, `organization_members_page`** — 20 new tests. No bugs found.
- [x] **`org_cost_fields_settings_page`, `org_join_codes_page`, `org_pending_approvals_page`** — 29 new tests. **Two real bugs found and fixed:** (1) `org_join_codes_page.dart`'s "Generate join code" success dialog wraps `QrImageView` inside `AlertDialog`'s `IntrinsicWidth`-wrapped content — `qr_flutter`'s `QrImageView` is a bare `LayoutBuilder`, whose intrinsic-dimension query `assert()`-throws in debug and silently collapses to zero width in release, so every org admin generating a join code hit this. Fixed by wrapping it in a fixed-size `SizedBox`, which answers the intrinsics query itself. (`hitchhiker_tab.dart`'s other `QrImageView` usage is inside a bottom sheet, not an `IntrinsicWidth` ancestor, so it was already safe — checked, not assumed.) (2) `org_cost_fields_settings_page.dart`'s org-wide default-approval-threshold card was nested inside the cost-fields list, which short-circuited to an empty-state message before ever reaching it — an org with zero cost fields configured had **no way to set its default approval threshold at all**. Fixed by always rendering the threshold card as list item 0, with the empty-state message becoming item 1 only when there are no fields.
- [x] **`splash_page`, `edit_profile_page`, `notification_preferences_page`, `public_profile_page`** — 24 new tests. No bugs found.

**Verification:** `flutter analyze --no-fatal-infos` — 11 issues, unchanged baseline (zero new warnings/errors across all 4 passes). `flutter test` — **1189/1189 passing** (+88 from the 1101 baseline above). `dart format lib/ test/` — clean. All 13 previously zero-coverage pages from the prior entry are now covered; `chat_page` remains the one deliberate exception (documented reason above). Committed as `4a5f11b`.

## macOS CocoaPods → Swift Package Manager migration (2026-08-13)

`macos/Podfile`, `Podfile.lock`, and `project.pbxproj` had been sitting modified-but-uncommitted since before this session's own changes began. Investigated rather than blindly committed, since the diff looked wrong on inspection: `Podfile.lock` had dropped the secure-storage pod entirely (down to just bare `FlutterMacOS`) and `project.pbxproj` was missing the "[CP] Embed Pods Frameworks" build phase — the step that actually copies CocoaPods frameworks into the app bundle. Confirmed via `flutter build macos --config-only`: every macOS plugin this project depends on (including `flutter_secure_storage_darwin`, used for auth/session token storage) now ships as a Swift Package, not a CocoaPods pod — the stale diff was CocoaPods correctly finding nothing left to manage, not broken/discarded work.

- [x] **Completed the migration per Flutter's own recommendation**: `pod deintegrate`, deleted `macos/Podfile`/`Podfile.lock`, removed the now-dangling `Pods-Runner` xcconfig includes from `macos/Flutter/Flutter-Debug.xcconfig`/`Flutter-Release.xcconfig`. `MACOSX_DEPLOYMENT_TARGET` bumped 10.15 → 12.0 (required by the current `FlutterMacOS` pod/engine — confirmed via CocoaPods' own compatibility warning, not guessed). iOS untouched — it still needs CocoaPods for `google_maps_flutter_ios`, which has no SPM support (see `macos/.gitignore`'s own comment on this asymmetry).
- [x] **Verified with a real build, not just config generation**: `flutter build macos --debug` succeeds, and `flutter_secure_storage_darwin`'s Dart code (19 `strings` hits in `kumo_claude.debug.dylib`) and native resource bundle are both confirmed present in the built app — the exact risk this fix targets (a missing-framework crash on first secure-storage access) is confirmed closed, not assumed.

**Verification:** `flutter analyze --no-fatal-infos` — 11 issues, unchanged baseline. `dart format` — clean. Committed as `172a3dd`.

## Flutter `integration_test` coverage added (2026-08-13)

This project had `flutter test` widget-test coverage (mocked providers/usecases, no network) and `scripts/web_smoke_test/` (live but pixel-tap-driven, web-renderer-only), but no real end-to-end coverage on native targets. This local dev environment turned out to have real device access (macOS desktop, Chrome, and a wirelessly-connected iPhone, plus bootable iOS simulators) — unlike the earlier "no attached display, no simulator" environment the `web_smoke_test` harness was built for — so Flutter's official `integration_test` package (real accessibility tree, `find.text`/`find.byType` instead of raw pixel coordinates) is usable here.

- [x] **Added `integration_test` (Flutter SDK package) to `pubspec.yaml` dev_dependencies.**
- [x] **New `integration_test/app_test.dart`** — one test, deliberately read-only and unauthenticated: boots the real app (`app.main()`, real `Supabase.initialize()`, real secure-storage session lookup), confirms it lands on the login page with no session found. Verified passing against a real `flutter build macos --debug` target with `--dart-define-from-file=env.local.json`. Doubles as a second, independent confirmation that the CocoaPods→SPM fix above works in a live running app, not just a static binary inspection.
- [x] **New `integration_test/README.md`** — how to run, and a safety-rules section mirroring `scripts/web_smoke_test/README.md`'s (written after this project's real-trip-deletion incident): no logging into a real personal account without explicit sign-off, dedicated test-account credentials via `--dart-define` if authenticated coverage is added, disposable `SMOKETEST-`-prefixed fixtures, no CI runs these (this project has none) so they're a manual local verification step.
- **Deliberately not attempted this pass**: authenticated-flow coverage (login, trip creation, etc.) — needs either a dedicated test account or explicit sign-off to use a real one, not decided unilaterally, consistent with how the live web-smoke-test scope question was handled earlier this session.

**Verification:** `flutter analyze --no-fatal-infos integration_test/` — 0 issues. `flutter test integration_test/app_test.dart -d macos --dart-define-from-file=env.local.json` — 1/1 passing. Not yet committed.
